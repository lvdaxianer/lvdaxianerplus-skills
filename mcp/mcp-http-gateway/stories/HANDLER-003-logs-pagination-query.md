# HANDLER-003: Logs Handler 分页查询

## 故事目标

- **核心问题**：验证 logs.handler.ts 能正确处理分页日志查询
- **目标用户**：Dashboard 用户
- **预期产出**：返回分页日志数据和 pagination 元数据

## 关键节点/里程碑

- **起点**：请求到达 /api/logs/paginated
- **中间步骤**：
  1. 解析 query 参数 (page, pageSize, startDate, endDate, tool)
  2. 调用数据库分页查询
  3. 获取日志数据
  4. 计算 pagination 信息
  5. 组装响应数据
- **终点**：返回 { data: logs, pagination: { page, pageSize, total, totalPages, hasMore } }

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| Logs Handler | 处理日志查询 | Query params | Paginated JSON |
| sqlite-logger | 数据库查询 | filters | Logs data |

## 流程/路径

### 流转条件
路由匹配 /api/logs/paginated

### 分支路径
- **条件A** → 有数据 → 返回日志列表
- **条件B** → 无数据 → 返回空数组

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| getQueryParam | url | value | URL parsing |
| getRequestLogsPaginated | params, filters | logs, pagination | Database query |
| sendJsonResponse | result | JSON | HTTP response |

## 异常/边界情况

### 正常分支测试场景

#### 表单验证
| 字段 | 非空验证 | 长度验证 | 格式验证 |
|------|----------|----------|----------|
| page | 默认 1 | >= 1 | 整数 |
| pageSize | 默认 20 | 1-100 | 整数 |
| startDate | 可选 | - | YYYY-MM-DD |
| endDate | 可选 | - | YYYY-MM-DD |

#### 业务流程分支
| 条件 | 参数 | 预期结果 |
|------|------|----------|
| page=1, pageSize=20 | 正常分页 | 第一页 20 条 |
| tool=getUser | 工具过滤 | 只返回 getUser 日志 |
| startDate=2026-01-01 | 时间过滤 | 返回指定日期后日志 |

### 异常分支测试场景

#### 输入异常
| 测试类型 | 字段 | 预期处理 |
|----------|------|----------|
| page < 1 | 无效页码 | 使用默认值 1 |
| pageSize > 100 | 超大 pageSize | 限制为 100 |
| 无效日期格式 | startDate | 忽略或报错 |

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 超过总页数 | page=100 | 返回空数组 |
| pageSize=0 | 无效值 | 使用默认 20 |

### 用户体验测试场景

#### 分页/翻页体验
| 场景 | 预期体验 |
|------|----------|
| 首页 | page=1, hasMore=true/false |
| 末页 | hasMore=false |
| 空数据 | data=[], total=0 |

### 超时处理
查询超时 30s 返回 500

### 错误恢复机制
查询失败返回空数组 + 错误信息

---

*Generated by 故事线技能 | Date: 2026-04-19*