# EJS-005: Scripts Partial JavaScript 加载

## 故事目标

- **核心问题**：验证 scripts.ejs 能正确注入 JavaScript 代码到页面
- **目标用户**：Dashboard 用户
- **预期产出**：所有 JS 函数（Toast、Modal、API helper）正确加载并可用

## 关键节点/里程碑

- **起点**：scripts.ejs 被 include 到主模板
- **中间步骤**：
  1. 注入 State 变量声明
  2. 注入 API helper 函数
  3. 注入 Toast 函数
  4. 注入 Confirm dialog 函数
  5. 注入各页面交互函数
  6. 注入事件监听器
- **终点**：页面 JS 环境完整初始化

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| Scripts Partial | 注入 JS | - | JavaScript code |
| Browser | 执行 JS | Script tag | Runtime functions |

## 流程/路径

### 流转条件
主模板执行到 scripts.ejs include 位置

### 分支路径
- **条件A** → JS 正常加载 → 函数可用
- **条件B** → JS 加载失败 → 页面无交互功能

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| showToast | message, type | DOM toast | Function call |
| openModal | toolName | DOM modal | Function call |
| api | path, method, body | JSON response | Fetch API |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 路径 | 预期结果 |
|------|------|----------|
| JS 加载成功 | 正常执行 | 所有函数可用 |
| 函数定义正确 | 调用成功 | 正常行为 |

### 异常分支测试场景

#### 输入异常
| 测试类型 | 字段 | 预期处理 |
|----------|------|----------|
| JS 语法错误 | script content | Console 报错 |

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 函数未定义 | call undefined fn | ReferenceError |

### 用户体验测试场景

#### 加载状态
| 场景 | 组件 | 预期体验 |
|------|------|----------|
| JS 加载中 | Script | DOMContentLoaded 后可用 |

### 超时处理
无

### 错误恢复机制
JS 错误时 console 报错，不影响页面显示

---

*Generated by 故事线技能 | Date: 2026-04-19*