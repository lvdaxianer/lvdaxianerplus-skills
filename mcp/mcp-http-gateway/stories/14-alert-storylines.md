# 告警通知功能故事线

> 测试覆盖：通知渠道、告警规则、冷却机制、风暴防护、历史记录

---

## STORY-AL-001: 告警初始化

### 故事目标
- **核心问题**：验证告警模块正确初始化
- **目标用户**：运维工程师
- **预期产出**：告警配置和数据库表创建成功

### 关键节点/里程碑
1. **起点**：调用 initAlert
2. **步骤1**：创建 alert_history 表
3. **步骤2**：加载告警配置
4. **步骤3**：初始化通知渠道
5. **终点**：告警模块就绪

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 正常初始化 | 有效配置 | 初始化成功 |
| 数据库连接失败 | 连接异常 | 初始化失败 |
| 配置缺失 | 无 alert 配置 | 使用默认配置 |

---

## STORY-AL-002: Webhook 通知渠道

### 故事目标
- **核心问题**：验证 Webhook 通知发送成功
- **目标用户**：运维工程师
- **预期产出**：告警消息发送到 Webhook URL

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 正常发送 | 有效 URL | 发送成功 |
| URL 无效 | 无效 URL | 记录失败日志 |
| 超时 | 连接超时 | 记录超时日志 |
| 返回错误 | HTTP 500 | 记录错误日志 |

---

## STORY-AL-003: Slack 通知渠道

### 故事目标
- **核心问题**：验证 Slack 通知发送成功
- **目标用户**：运维工程师
- **预期产出**：告警消息发送到 Slack Webhook

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 正常发送 | 有效 Webhook | Slack 收到消息 |
| 格式正确 | Slack 格式 | 消息显示正常 |
| Webhook 无效 | 无效 URL | 记录失败日志 |

---

## STORY-AL-004: 钉钉通知渠道

### 故事目标
- **核心问题**：验证钉钉通知发送成功
- **目标用户**：运维工程师
- **预期产出**：告警消息发送到钉钉 Webhook

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 正常发送 | 有效 Webhook | 钉钉收到消息 |
| 签名验证 | 正确签名 | 签名通过 |
| Webhook 无效 | 无效 URL | 记录失败日志 |

---

## STORY-AL-005: 企业微信通知渠道

### 故事目标
- **核心问题**：验证企业微信通知发送成功
- **目标用户**：运维工程师
- **预期产出**：告警消息发送到企业微信 Webhook

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 正常发送 | 有效 Webhook | 企业微信收到消息 |
| 格式正确 | Markdown 格式 | 消息显示正常 |
| Webhook 无效 | 无效 URL | 记录失败日志 |

---

## STORY-AL-006: 熔断器状态变更告警

### 故事目标
- **核心问题**：验证熔断器状态变更触发告警
- **目标用户**：运维工程师
- **预期产出**：熔断器 OPEN 时发送告警

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| CLOSED → OPEN | 状态变更 | 发送告警 |
| OPEN → HALF_OPEN | 状态变更 | 发送恢复告警 |
| HALF_OPEN → CLOSED | 状态变更 | 发送恢复告警 |
| 无状态变更 | 状态不变 | 不发送告警 |

---

## STORY-AL-007: 限流拒绝告警

### 故事目标
- **核心问题**：验证限流拒绝触发告警
- **目标用户**：运维工程师
- **预期产出**：限流拒绝超过阈值发送告警

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 拒绝超阈值 | 拒绝次数 > threshold | 发送告警 |
| 拒绝未超阈值 | 拒绝次数 < threshold | 不发送告警 |
| 限流禁用 | 无限流 | 不触发告警 |

---

## STORY-AL-008: 并发超限告警

### 故事目标
- **核心问题**：验证并发超限触发告警
- **目标用户**：运维工程师
- **预期产出**：并发超限发送告警

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 并发超限 | 当前并发 > 最大值 | 发送告警 |
| 并发正常 | 当前并发 < 最大值 | 不发送告警 |
| 队列满 | 等待队列满 | 发送告警 |

---

## STORY-AL-009: 错误率阈值告警

### 故事目标
- **核心问题**：验证错误率超过阈值触发告警
- **目标用户**：运维工程师
- **预期产出**：错误率 > threshold 发送告警

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 错误率超阈值 | 错误率 > 10% | 发送告警 |
| 错误率正常 | 错误率 < 10% | 不发送告警 |
| 无请求 | 请求数为 0 | 不触发告警 |

---

## STORY-AL-010: 超时告警

### 故事目标
- **核心问题**：验证超时触发告警
- **目标用户**：运维工程师
- **预期产出**：超时次数超阈值发送告警

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 超时超阈值 | 超时次数 > threshold | 发送告警 |
| 超时未超阈值 | 超时次数 < threshold | 不发送告警 |
| 无超时 | 超时次数为 0 | 不发送告警 |

---

## STORY-AL-011: 告警冷却机制

### 故事目标
- **核心问题**：验证告警冷却防止重复告警
- **目标用户**：运维工程师
- **预期产出**：冷却期内不重复发送告警

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 冷却期内 | 上次告警 < cooldown | 不发送告警 |
| 冷却期过 | 上次告警 > cooldown | 发送告警 |
| 不同告警类型 | 不同类型 | 独立冷却 |

---

## STORY-AL-012: 告警风暴防护

### 故事目标
- **核心问题**：验证告警风暴防护机制
- **目标用户**：运维工程师
- **预期产出**：每小时最大告警数限制生效

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 未达上限 | 告警数 < maxPerHour | 正常发送 |
| 达到上限 | 告警数 = maxPerHour | 停止发送 |
| 超过上限 | 告警数 > maxPerHour | 拒绝发送 |
| 计数重置 | 下一小时 | 计数清零 |

---

## STORY-AL-013: 告警历史记录

### 故事目标
- **核心问题**：验证告警历史正确记录
- **目标用户**：运维工程师
- **预期产出**：告警记录保存到 alert_history 表

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 告警发送成功 | 成功发送 | 记录 status=success |
| 告警发送失败 | 发送失败 | 记录 status=failed |
| 告警被冷却 | 冷却期 | 记录 status=cooldown |
| 告警被风暴防护 | 达到上限 | 记录 status=storm_protected |

---

## STORY-AL-014: 告警历史查询

### 故事目标
- **核心问题**：验证 `/api/alert/history` 正确返回历史记录
- **目标用户**：Dashboard 用户
- **预期产出**：返回告警历史列表

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 默认查询 | 无参数 | 返回最近 50 条 |
| 分页查询 | limit=100 | 返回 100 条 |
| 按类型筛选 | type=circuitBreaker | 返回熔断器告警 |
| 按状态筛选 | status=success | 返回成功告警 |

---

## STORY-AL-015: 告警配置查询

### 故事目标
- **核心问题**：验证 `/api/alert/rules` 返回告警规则
- **目标用户**：Dashboard 用户
- **预期产出**：返回当前告警规则配置

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 查询规则 | GET /api/alert/rules | 返回规则列表 |
| 查询渠道 | GET /api/alert/channels | 返回渠道列表 |
| 查询配置 | GET /api/alert | 返回完整配置 |

---

## STORY-AL-016: 告警模板

### 故事目标
- **核心问题**：验证告警模板正确渲染
- **目标用户**：运维工程师
- **预期产出**：告警消息按模板格式化

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 默认模板 | 无自定义 | 使用默认格式 |
| 自定义模板 | 提供模板 | 使用自定义格式 |
| 模板变量替换 | {alertType} | 替换为实际值 |

---

## STORY-AL-017: AlertConfig 类型

### 故事目标
- **核心问题**：验证 EnhancedAlertConfig 类型完整性
- **目标用户**：开发者
- **预期产出**：配置类型正确定义

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| enabled 字段 | boolean | 启用/禁用 |
| channels 字段 | AlertChannelConfig[] | 通知渠道列表 |
| rules 字段 | AlertRuleConfig[] | 告警规则列表 |
| cooldown 字段 | number | 冷却时间（秒） |
| maxPerHour 字段 | number | 每小时上限 |

---

## STORY-AL-018: AlertChannelConfig 类型

### 故事目标
- **核心问题**：验证 AlertChannelConfig 类型完整性
- **目标用户**：开发者
- **预期产出**：渠道配置正确定义

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| type 字段 | webhook/slack/dingtalk/wechat | 渠道类型 |
| url 字段 | string | Webhook URL |
| enabled 字段 | boolean | 启用/禁用 |
| secret 字段 | string? | 签名密钥（可选） |

---

## STORY-AL-019: AlertRuleConfig 类型

### 故事目标
- **核心问题**：验证 AlertRuleConfig 类型完整性
- **目标用户**：开发者
- **预期产出**：规则配置正确定义

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| type 字段 | ruleType | 规则类型 |
| threshold 字段 | number | 触发阈值 |
| enabled 字段 | boolean | 启用/禁用 |
| cooldown 字段 | number? | 冷却时间（可选） |

---

## STORY-AL-020: 告警历史清理

### 故事目标
- **核心问题**：验证告警历史自动清理
- **目标用户**：运维工程师
- **预期产出**：过期告警记录自动删除

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 自动清理 | 定时清理 | 删除过期记录 |
| 保留天数 | retentionDays=7 | 保留 7 天 |
| 手动清理 | cleanAlertHistory | 清理成功 |

---

## STORY-AL-021: 多渠道并行发送

### 故事目标
- **核心问题**：验证多渠道并行发送告警
- **目标用户**：运维工程师
- **预期产出**：告警同时发送到多个渠道

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 多渠道启用 | 2+ 渠道启用 | 并行发送 |
| 部分渠道失败 | 1 渠道失败 | 其他渠道成功 |
| 非阻塞发送 | 异步发送 | 不阻塞主流程 |

---

## STORY-AL-022: 告警消息格式

### 故事目标
- **核心问题**：验证告警消息格式正确
- **目标用户**：运维工程师
- **预期产出**：消息包含必要信息

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 消息标题 | alertType | 包含告警类型 |
| 消息内容 | details | 包含详细信息 |
| 时间戳 | timestamp | 包含告警时间 |
| 来源标识 | source | 包含来源信息 |

---

## STORY-AL-023: 告警统计信息

### 故事目标
- **核心问题**：验证 getAlertStats 返回正确统计
- **目标用户**：Dashboard 用户
- **预期产出**：返回告警统计信息

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 总告警数 | totalAlerts | 总计数量 |
| 成功告警数 | successCount | 成功数量 |
| 失败告警数 | failedCount | 失败数量 |
| 冷却告警数 | cooldownCount | 冷却数量 |

---

## STORY-AL-024: 告警禁用场景

### 故事目标
- **核心问题**：验证告警禁用时不发送告警
- **目标用户**：配置管理员
- **预期产出**：禁用后不发送告警

### 测试场景

| 条件 | 配置 | 预期结果 |
|------|------|----------|
| 全局禁用 | enabled=false | 不发送任何告警 |
| 渠道禁用 | channel.enabled=false | 该渠道不发送 |
| 规则禁用 | rule.enabled=false | 该规则不触发 |

---

## STORY-AL-025: 告警错误恢复

### 故事目标
- **核心问题**：验证告警发送失败后能恢复
- **目标用户**：运维工程师
- **预期产出**：发送失败不影响后续告警

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 单次失败 | 1 次失败 | 记录日志，后续正常 |
| 连续失败 | 多次失败 | 记录日志，后续正常 |
| 渠道恢复 | 渠道恢复可用 | 恢复发送 |

---

*Generated by 故事线技能 | Date: 2026-04-22*