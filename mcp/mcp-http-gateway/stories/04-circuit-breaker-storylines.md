# 熔断器模块故事线

## 故事线概览

本模块包含 10 条故事线，覆盖熔断器的核心功能。

---

## 故事线 46：熔断器状态模型

### 故事目标
- **核心问题**：如何实现 CLOSED → OPEN → HALF_OPEN 三态模型
- **目标用户**：系统管理员
- **预期产出**：正确的状态流转

### 关键节点
1. **起点**：初始 CLOSED 状态
2. **中间步骤**：
   - 失败计数达到阈值 → OPEN
   - 恢复时间后 → HALF_OPEN
   - 成功计数达到阈值 → CLOSED
3. **终点**：状态稳定

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| StateManager | 状态管理 | 事件 | 新状态 |
| Counter | 计数器 | 成功/失败 | 计数值 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 失败计数 | failure | failureCount | increment |
| 状态转换 | threshold | newState | setState |
| 定时恢复 | halfOpenTime | transition | setTimeout |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 快速恢复 | HALF_OPEN 立即成功 | 转 CLOSED |
| 恢复失败 | HALF_OPEN 仍失败 | 转 OPEN |

---

## 故事线 47：失败阈值触发

### 故事目标
- **核心问题**：如何在失败次数达到阈值时触发熔断
- **目标用户**：系统自身
- **预期产出**：OPEN 状态触发

### 关键节点
1. **起点**：请求连续失败
2. **中间步骤**：
   - 计数失败次数
   - 检查是否达到阈值
   - 触发状态转换
   - 记录告警
3. **终点**：熔断器 OPEN

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| FailureCounter | 失败计数 | failure | count |
| ThresholdChecker | 阈值检查 | count | shouldTrip |
| AlertLogger | 告警记录 | OPEN 状态 | 告警日志 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 计数增加 | failure | count++ | increment |
| 阈值比较 | count, threshold | shouldTrip | >= |
| 状态转换 | shouldTrip | OPEN | setState |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 部分成功 | 失败间有成功 | 重置计数 |
| 阈值配置错误 | threshold <= 0 | 使用默认值 |

---

## 故事线 48：恢复时间等待

### 故事目标
- **核心问题**：如何在 OPEN 状态等待恢复时间后转为 HALF_OPEN
- **目标用户**：系统自身
- **预期产出**：HALF_OPEN 状态触发

### 关键节点
1. **起点**：OPEN 状态
2. **中间步骤**：
   - 记录 OPEN 时间
   - 等待 halfOpenTime
   - 转换为 HALF_OPEN
   - 允许有限尝试
3. **终点**：HALF_OPEN 状态

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| Timer | 定时器 | halfOpenTime | 转换信号 |
| HalfOpenTransitioner | 半开转换 | 定时触发 | HALF_OPEN |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 时间记录 | openTime | storedTime | Date.now |
| 定时等待 | halfOpenTime | ready | setTimeout |
| 状态转换 | ready | HALF_OPEN | setState |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 配置错误 | halfOpenTime <= 0 | 使用默认 30s |
| 手动恢复 | 外部触发 | 立即转换 |

---

## 故事线 49：成功阈值恢复

### 故事目标
- **核心问题**：如何在成功次数达到阈值时恢复正常
- **目标用户**：系统自身
- **预期产出**：CLOSED 状态恢复

### 关键节点
1. **起点**：HALF_OPEN 状态
2. **中间步骤**：
   - 计数成功次数
   - 检查是否达到阈值
   - 触发状态转换
   - 重置计数器
3. **终点**：熔断器 CLOSED

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| SuccessCounter | 成功计数 | success | count |
| ThresholdChecker | 阈值检查 | count | shouldRecover |
| CounterReset | 计数重置 | CLOSED | reset |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 计数增加 | success | count++ | increment |
| 阈值比较 | count, threshold | shouldRecover | >= |
| 状态转换 | shouldRecover | CLOSED | setState |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 中途失败 | HALF_OPEN 中失败 | 转 OPEN |
| 阈值配置错误 | threshold <= 0 | 使用默认值 |

---

## 故事线 50：OPEN 状态拦截

### 故事目标
- **核心问题**：如何在 OPEN 状态拦截请求
- **目标用户**：Executor
- **预期产出**：请求被拦截，触发回退

### 关键节点
1. **起点**：请求到达
2. **中间步骤**：
   - 检查熔断器状态
   - 状态为 OPEN
   - 拒绝请求
   - 触发回退链
3. **终点**：回退响应

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| StateChecker | 状态检查 | toolName | state |
| RequestBlocker | 请求拦截 | OPEN | 拒绝 |
| FallbackTrigger | 回退触发 | 拒绝 | 回退链 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 状态查询 | toolName | state | getState |
| 拦截判断 | state == OPEN | blocked | 条件判断 |
| 回退调用 | blocked | fallbackResult | executeFallbackChain |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 状态查询失败 | 无此工具记录 | 默认 CLOSED |
| HALF_OPEN | 半开状态 | 允许尝试 |

---

## 故事线 51：HALF_OPEN 尝试

### 故事目标
- **核心问题**：如何在 HALF_OPEN 状态允许有限尝试
- **目标用户**：系统自身
- **预期产出**：尝试成功或失败决定状态

### 关键节点
1. **起点**：HALF_OPEN 状态
2. **中间步骤**：
   - 允许请求通过
   - 记录结果
   - 成功 → 计数成功
   - 失败 → 转 OPEN
3. **终点**：状态转换

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| RequestAllower | 请求允许 | HALF_OPEN | 通过 |
| ResultRecorder | 结果记录 | success/failure | 计数 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 状态检查 | state | HALF_OPEN | 条件判断 |
| 请求通过 | allow | response | HTTP 请求 |
| 结果记录 | response | stateChange | 计数检查 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 尝试失败 | 请求失败 | 转 OPEN |
| 多次尝试 | 多请求并发 | 限制并发 |

---

## 故事线 52：工具级熔断器

### 故事目标
- **核心问题**：如何为每个工具维护独立熔断器
- **目标用户**：系统管理员
- **预期产出**：工具隔离的熔断状态

### 关键节点
1. **起点**：多个工具配置
2. **中间步骤**：
   - 创建工具熔断器 Map
   - 每个工具独立计数
   - 独立状态管理
   - 独立恢复时间
3. **终点**：工具级熔断器

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| CircuitStore | 存储 | toolName | 状态对象 |
| StateMap | 状态 Map | 工具列表 | 状态集合 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| Map 初始化 | tools | stateMap | new Map |
| 状态查询 | toolName | state | Map.get |
| 状态更新 | toolName, state | result | Map.set |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 工具不存在 | Map 无此 key | 创建默认状态 |
| Map 清理 | 工具删除 | 删除对应状态 |

---

## 故事线 53：熔断器配置

### 故事目标
- **核心问题**：如何配置熔断器参数
- **目标用户**：系统管理员
- **预期产出**：灵活的熔断器配置

### 关键节点
1. **起点**：circuitBreaker 配置
2. **中间步骤**：
   - 解析 failureThreshold
   - 解析 successThreshold
   - 解析 halfOpenTime
   - 应用配置
3. **终点**：熔断器参数生效

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| ConfigResolver | 配置解析 | config.circuitBreaker | 参数对象 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 失败阈值读取 | failureThreshold | thresholdValue | 直接读取 |
| 成功阈值读取 | successThreshold | thresholdValue | 直接读取 |
| 恢复时间读取 | halfOpenTime | timeValue | 直接读取 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 配置缺失 | circuitBreaker 未定义 | 使用默认值 |
| 参数非法 | threshold <= 0 | 使用默认值 |

---

## 故事线 54：熔断器监控

### 故事目标
- **核心问题**：如何监控熔断器状态
- **目标用户**：运维人员
- **预期产出**：熔断器状态可视化

### 关键节点
1. **起点**：Dashboard 请求
2. **中间步骤**：
   - 遍历所有工具熔断器
   - 汇总状态统计
   - 提供状态 API
   - Dashboard 展示
3. **终点**：熔断器状态可视化

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| StateCollector | 状态收集 | stateMap | 状态列表 |
| StatsCalculator | 统计计算 | 状态列表 | 统计数据 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 状态遍历 | stateMap | states | Map.forEach |
| 状态统计 | states | stats | count |
| API 返回 | stats | JSON | JSON.stringify |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 无熔断器 | stateMap 为空 | 返回空统计 |
| 状态异常 | state 值非法 | 跳过该工具 |

---

## 故事线 55：熔断器告警

### 故事目标
- **核心问题**：如何在熔断器状态变化时发出告警
- **目标用户**：运维人员
- **预期产出**：及时的告警通知

### 关键节点
1. **起点**：状态转换事件
2. **中间步骤**：
   - 检测状态变化
   - 记录告警日志
   - 写入 alert_logs
   - 可选外部通知
3. **终点**：告警发出

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| StateChangeDetector | 变化检测 | oldState, newState | changeEvent |
| AlertWriter | 告警写入 | changeEvent | alertLog |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 变化检测 | state | changed | 比较运算 |
| 告警类型 | transition | alertType | CLOSED/OPEN/HALF_OPEN |
| 日志写入 | alertType | logEntry | database.write |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 无变化 | oldState == newState | 不告警 |
| 写入失败 | database 错误 | 缓存重试 |

---

*Generated by 故事线技能 | Date: 2026-04-19*