# ROUTER-001: 路由策略表注册与排序

## 故事目标

- **核心问题**：验证 RouterStrategyTable 能正确注册路由并按优先级排序
- **目标用户**：开发者/测试人员
- **预期产出**：路由按优先级降序排列，高优先级路由优先匹配

## 关键节点/里程碑

- **起点**：调用 router.register(strategy)
- **中间步骤**：
  1. 接收 RouteStrategy 对象
  2. 检查 priority 字段
  3. 未设置时使用默认值 0
  4. 添加到 routes 数组
  5. 按优先级降序排序
- **终点**：routes 数组按优先级排序完成

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| RouterStrategyTable | 注册路由 | RouteStrategy | Sorted routes array |
| RouteStrategy | 路由配置 | name, path, handler | Configuration object |

## 流程/路径

### 流转条件
调用 register() 方法

### 分支路径
- **条件A** → priority 已设置 → 直接添加
- **条件B** → priority 未设置 → 使用默认 0

### 循环/回退机制
每次 register 后重新排序

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| register | strategy | sorted array | Method call |
| sort | routes | sorted routes | Array sort |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 路径 | 预期结果 |
|------|------|----------|
| priority = 60 | 高优先级 | 排在数组前部 |
| priority = 0 | 默认优先级 | 排在数组后部 |
| priority = -100 | 低优先级 | 排在数组末尾 |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 相同优先级 | priority = 50, 50 | 保持原顺序 |
| 负数优先级 | priority = -100 | 排在末尾 |
| 极大优先级 | priority = 1000 | 排在首位 |

### 超时处理
无

### 错误恢复机制
无效 strategy 时跳过

---

*Generated by 故事线技能 | Date: 2026-04-19*