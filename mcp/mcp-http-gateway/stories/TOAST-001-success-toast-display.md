# TOAST-001: Toast 成功提示显示

## 故事目标

- **核心问题**：验证 Toast 组件能正确显示成功类型提示
- **目标用户**：Dashboard 用户
- **预期产出**：操作成功后显示绿色 Toast，3 秒后自动消失

## 关键节点/里程碑

- **起点**：调用 showToast(message, 'success')
- **中间步骤**：
  1. 创建 toast DOM 元素
  2. 设置 class 为 'toast toast-success'
  3. 设置背景色 #DCFCE7，文字色 #166534
  4. 添加到 toast-container
  5. 3 秒后自动移除
- **终点**：Toast 显示并消失

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| showToast | 创建 Toast | message, type | DOM element |
| toast-container | 容器 | - | Toast 位置 |
| setTimeout | 自动移除 | 3000ms | 移除元素 |

## 流程/路径

### 流转条件
操作成功需要提示用户

### 分支路径
- **条件A** → type = 'success' → 绿色 Toast
- **条件B** → 其他 type → 其他颜色

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| showToast | '操作成功', 'success' | DOM toast | Function call |
| container.appendChild | toast | 显示 | DOM API |
| setTimeout | 3000 | 移除 | Timer |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 参数 | 预期结果 |
|------|------|----------|
| showToast('保存成功', 'success') | 成功 Toast | 绿色背景显示 3s |
| showToast('数据已刷新', 'success') | 成功 Toast | 绿色背景显示 3s |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 空消息 | message = '' | 显示空 Toast |
| 超长消息 | message > 50 chars | 截断或换行 |
| 多次调用 | 连续 5 次 showToast | 5 个 Toast 堆叠 |

### 用户体验测试场景

#### 提示与反馈
| 场景 | 预期体验 |
|------|----------|
| 操作成功 | Toast 显示在右下角 |
| 3 秒后 | Toast 自动消失 |

#### 加载状态
| 场景 | 组件 | 预期体验 |
|------|------|----------|
| Toast 显示 | toast-container | 位置固定在右下角 |

### 超时处理
Toast 固定 3 秒消失

### 错误恢复机制
Toast DOM 错误时不影响主流程

---

*Generated by 故事线技能 | Date: 2026-04-19*