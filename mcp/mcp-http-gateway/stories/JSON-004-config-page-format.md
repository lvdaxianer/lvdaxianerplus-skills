# JSON-004: Config 页面 JSON 格式化

## 故事目标

- **核心问题**：验证 Config 页面的 JSON 格式化功能
- **目标用户**：Dashboard 用户
- **预期产出**：配置编辑器的 JSON 能正确格式化

## 关键节点/里程碑

- **起点**：config-editor textarea 包含配置 JSON
- **中间步骤**：
  1. 点击"格式化"按钮
  2. formatConfigJson() 执行
  3. JSON.parse + stringify
  4. 更新 config-editor.value
  5. 隐藏 config-error 提示
- **终点**：配置 JSON 格式化，无错误提示

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| formatConfigJson | 格式化配置 | editor value | formatted |
| config-error | 错误提示 | display | none |

## 流程/路径

### 流转条件
用户在 Config 页面点击格式化

### 分支路径
- **条件A** → JSON 有效 → 格式化 + 隐藏错误
- **条件B** → JSON 无效 → 显示错误信息

---

*Generated by 故事线技能 | Date: 2026-04-19*