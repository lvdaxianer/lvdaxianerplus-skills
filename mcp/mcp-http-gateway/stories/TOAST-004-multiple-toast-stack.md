# TOAST-004: Toast 多个堆叠显示

## 故事目标

- **核心问题**：验证多个 Toast 能正确堆叠显示不互相干扰
- **目标用户**：Dashboard 用户
- **预期产出**：多个 Toast 同时显示，各自独立消失

## 关键节点/里程碑

- **起点**：连续调用 showToast 多次
- **中间步骤**：
  1. 第一个 Toast 创建并添加
  2. 第二个 Toast 创建并添加（堆叠）
  3. 第三个 Toast 创建并添加（继续堆叠）
  4. 各 Toast 独立计时
  5. 各 Toast 独立消失
- **终点**：所有 Toast 按顺序消失

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| showToast | 创建 Toast | message, type | DOM element |
| toast-container | 堆叠容器 | - | 多个 Toast |

## 流程/路径

### 流转条件
短时间内多次操作触发

### 分支路径
- **条件A** → 连续调用 → Toast 堆叠
- **条件B** → 间隔 > 3s → Toast 不重叠

### 循环/回退机制
每个 Toast 独立 setTimeout

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| showToast #1 | '保存成功' | toast1 | DOM appendChild |
| showToast #2 | '数据刷新' | toast2 | DOM appendChild |
| showToast #3 | '缓存清空' | toast3 | DOM appendChild |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 参数 | 预期结果 |
|------|------|----------|
| 连续 3 次调用 | 不同消息 | 3 个 Toast 堆叠 |
| 不同类型 | success, error, info | 不同颜色堆叠 |

### 异常分支测试场景

#### 并发测试
| 测试场景 | 操作 | 预期行为 |
|----------|------|----------|
| 快速连续 | 10 次 showToast | 10 个 Toast 堆叠 |
| 同一时刻 | 多按钮触发 | 各 Toast 独立显示 |

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 大量 Toast | 20 个 Toast | 可能溢出容器 |
| 容器高度 | fixed height | 限制堆叠数量 |

### 用户体验测试场景

#### 提示与反馈
| 场景 | 预期体验 |
|------|----------|
| 多 Toast | 右下角堆叠显示 |
| 各独立消失 | 各 3 秒后消失 |

### 超时处理
各 Toast 独立 3 秒计时

### 错误恢复机制
Toast 过多时限制数量

---

*Generated by 故事线技能 | Date: 2026-04-19*