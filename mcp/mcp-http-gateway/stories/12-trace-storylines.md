# 链路追踪功能故事线

> 测试覆盖：Trace ID 生成、传递、查询 API、数据库持久化

---

## STORY-TR-001: Trace ID 基础生成

### 故事目标
- **核心问题**：验证 Trace ID 能正确生成唯一 ID
- **目标用户**：运维工程师
- **预期产出**：每次请求生成唯一 Trace ID

### 关键节点/里程碑
1. **起点**：请求到达网关
2. **步骤1**：检查请求头是否有 Trace ID
3. **步骤2**：无则生成新 ID
4. **终点**：Trace ID 赋值到请求上下文

### 测试场景

#### 正常分支
| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| UUID 格式 | 默认配置 | 标准 UUID 格式 |
| 短 ID 格式 | `generateShort: true` | 短 ID（约16字符） |
| 唯一性 | 多次生成 | 所有 ID 不重复 |

---

## STORY-TR-002: Trace ID Header 名称配置

### 故事目标
- **核心问题**：验证自定义 Header 名称生效
- **目标用户**：配置管理员
- **预期产出**：Trace ID 使用自定义 Header 名称

### 测试场景

| 条件 | 配置 | 预期结果 |
|------|------|----------|
| 默认名称 | `headerName: "X-Trace-ID"` | 使用默认 |
| 自定义名称 | `headerName: "X-Request-Id"` | 使用自定义 |
| 无配置 | 不设置 headerName | 使用默认 "X-Trace-ID" |

---

## STORY-TR-003: Trace ID 响应头返回

### 故事目标
- **核心问题**：验证 Trace ID 在响应头中返回
- **目标用户**：API 消费者
- **预期产出**：响应头包含 Trace ID

### 测试场景

| 条件 | 配置 | 预期结果 |
|------|------|----------|
| 启用返回 | `includeInResponse: true` | 响应头含 Trace ID |
| 禁用返回 | `includeInResponse: false` | 响应头不含 Trace ID |
| 默认行为 | 未配置 | 默认启用 |

---

## STORY-TR-004: Trace ID 后端传递

### 故事目标
- **核心问题**：验证 Trace ID 传递到后端服务
- **目标用户**：分布式系统开发者
- **预期产出**：后端请求携带 Trace ID

### 测试场景

| 条件 | 配置 | 预期结果 |
|------|------|----------|
| 启用传递 | `propagateToBackend: true` | 后端收到 Trace ID |
| 禁用传递 | `propagateToBackend: false` | 后端无 Trace ID |
| Header 名称一致 | 自定义 headerName | 后端使用相同名称 |

---

## STORY-TR-005: Trace ID 请求头继承

### 故事目标
- **核心问题**：验证上游请求的 Trace ID 能继承
- **目标用户**：分布式系统开发者
- **预期产出**：使用上游 Trace ID 而非生成新 ID

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 请求头已有 Trace ID | Header: `X-Trace-ID: existing-id` | 使用 existing-id |
| 请求头无 Trace ID | 无 Header | 生成新 ID |
| 自定义名称继承 | Header: `X-Request-Id: custom-id` | 使用 custom-id |

---

## STORY-TR-006: Trace 日志记录

### 故事目标
- **核心问题**：验证 Trace ID 正确记录到日志
- **目标用户**：运维工程师
- **预期产出**：日志包含 Trace ID 字段

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 请求日志 | request_logs 表 | 包含 trace_id 字段 |
| 错误日志 | error_logs 表 | 包含 trace_id 字段 |
| Trace 日志 | trace_logs 表 | 包含完整 Trace 记录 |

---

## STORY-TR-007: Trace 数据库表结构

### 故事目标
- **核心问题**：验证 trace_logs 表正确创建和写入
- **目标用户**：数据库管理员
- **预期产出**：表结构完整，数据写入成功

### 测试场景

| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 表创建 | initTrace 后 | trace_logs 表存在 |
| 字段完整性 | 查询表结构 | id、trace_id、tool_name、timestamp 等字段 |
| 索引存在 | 查询索引 | trace_id、timestamp 索引 |

---

## STORY-TR-008: Trace API - 查询指定 Trace

### 故事目标
- **核心问题**：验证 `/api/trace/:id` 能正确查询 Trace 详情
- **目标用户**：Dashboard 用户
- **预期产出**：返回指定 Trace 的完整信息

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 有效 Trace ID | 存在的 ID | 返回 Trace 详情 |
| 无效 Trace ID | 不存在的 ID | 返回 404 |
| Trace 详情 | 查询结果 | 包含 tool_name、start_time、end_time、duration |

---

## STORY-TR-009: Trace API - 查询最近 Trace

### 故事目标
- **核心问题**：验证 `/api/trace/recent` 能返回最近 Trace 列表
- **目标用户**：Dashboard 用户
- **预期产出**：返回按时间倒序的 Trace 列表

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 默认数量 | 无参数 | 返回最近 50 条 |
| 自定义数量 | `limit=100` | 返回最近 100 条 |
| 空数据库 | 无 Trace 记录 | 返回空数组 |

---

## STORY-TR-010: Trace API - 按工具查询

### 故事目标
- **核心问题**：验证 `/api/trace/tool/:name` 能按工具名查询
- **目标用户**：Dashboard 用户
- **预期产出**：返回指定工具的 Trace 列表

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 有效工具名 | 存在的工具 | 返回该工具的 Trace |
| 无效工具名 | 不存在的工具 | 返回空数组 |
| 工具名大小写 | `getUser` vs `GetUser` | 区分大小写 |

---

## STORY-TR-011: Trace API - 统计信息

### 故事目标
- **核心问题**：验证 `/api/trace/stats` 能返回 Trace 统计
- **目标用户**：Dashboard 用户
- **预期产出**：返回活跃 Trace 数量等统计

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 统计字段 | 查询结果 | totalTraces、activeTraces、avgDuration |
| 实时更新 | 新请求后统计 | 统计数据更新 |

---

## STORY-TR-012: Trace 生命周期追踪

### 故事目标
- **核心问题**：验证 Trace 从开始到结束完整记录
- **目标用户**：运维工程师
- **预期产出**：完整 Trace 生命周期数据

### 测试场景

| 阶段 | 测试值 | 预期结果 |
|------|--------|----------|
| 开始 | startTrace | 记录 start_time |
| 结束 | endTrace | 记录 end_time、duration |
| 状态 | 查询结果 | 包含 status 字段 |

---

## STORY-TR-013: Trace 禁用场景

### 故事目标
- **核心问题**：验证 Trace 禁用时不生成 ID
- **目标用户**：配置管理员
- **预期产出**：禁用后请求无 Trace ID

### 测试场景

| 条件 | 配置 | 预期结果 |
|------|------|----------|
| 全局禁用 | `enabled: false` | 无 Trace ID |
| 部分禁用 | 特定工具禁用 | 该工具无 Trace ID |

---

## STORY-TR-014: Trace 并发请求测试

### 故事目标
- **核心问题**：验证高并发下 Trace ID 唯一性
- **目标用户**：性能工程师
- **预期产出**：100 并发请求 ID 不重复

### 测试场景

| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 100 并发 | 同时请求 | 100 个唯一 ID |
| 1000 并发 | 高并发测试 | 无 ID 重复 |

---

## STORY-TR-015: Trace 性能影响测试

### 故事目标
- **核心问题**：验证 Trace 对请求性能影响
- **目标用户**：性能工程师
- **预期产出**：Trace 开销 < 1ms

### 测试场景

| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| ID 生成耗时 | UUID 生成 | < 0.1ms |
| 数据库写入耗时 | trace_logs 写入 | < 1ms |
| 总体开销 | 启用 vs 禁用 | 响应时间差 < 5ms |

---

*Generated by 故事线技能 | Date: 2026-04-22*