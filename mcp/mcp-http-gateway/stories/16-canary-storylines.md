# 灰度发布功能故事线

> 测试覆盖：灰度策略、状态管理、指标监控、自动回滚、API 端点

---

## STORY-CR-001: 灰度发布初始化

### 故事目标
- **核心问题**：验证灰度发布模块正确初始化
- **目标用户**：运维工程师
- **预期产出**：canary_releases 和 canary_metrics 表创建成功

### 关键节点/里程碑
1. **起点**：调用 initCanary
2. **步骤1**：创建 canary_releases 表
3. **步骤2**：创建 canary_metrics 表
4. **步骤3**：加载活跃灰度发布
5. **终点**：灰度发布模块就绪

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 正常初始化 | 表不存在 | 创建成功 |
| 表已存在 | 表已创建 | 跳过创建 |
| 数据库失败 | 连接异常 | 初始化失败 |

---

## STORY-CR-002: 创建灰度发布

### 故事目标
- **核心问题**：验证 `/api/canary/create` 正确创建灰度发布
- **目标用户**：运维工程师
- **预期产出**：灰度发布创建成功，返回发布 ID

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 创建百分比策略 | percentage=10 | 创建成功 |
| 创建用户策略 | userIds=["user1"] | 创建成功 |
| 创建工具策略 | toolNames=["tool1"] | 创建成功 |
| 无策略参数 | 缺少 strategy | 创建失败 |

---

## STORY-CR-003: 百分比灰度策略

### 故事目标
- **核心问题**：验证百分比策略正确分流
- **目标用户**：运维工程师
- **预期产出**：按百分比路由请求到灰度版本

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 百分比=10% | 10% 请求 | 10% 到灰度 |
| 百分比=50% | 50% 请求 | 50% 到灰度 |
| 百分比=100% | 100% 请求 | 全部到灰度 |
| 百分比=0% | 0% 请求 | 全部到基线 |

---

## STORY-CR-004: 用户灰度策略

### 故事目标
- **核心问题**：验证用户策略正确分流
- **目标用户**：运维工程师
- **预期产出**：指定用户路由到灰度版本

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 用户在列表 | userId 在列表 | 路由到灰度 |
| 用户不在列表 | userId 不在列表 | 路由到基线 |
| 空用户列表 | userIds=[] | 全部到基线 |
| 用户 ID 大小写 | user1 vs User1 | 区分大小写 |

---

## STORY-CR-005: 工具灰度策略

### 故事目标
- **核心问题**：验证工具策略正确分流
- **目标用户**：运维工程师
- **预期产出**：指定工具路由到灰度版本

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 工具在列表 | toolName 在列表 | 路由到灰度 |
| 工具不在列表 | toolName 不在列表 | 路由到基线 |
| 空工具列表 | toolNames=[] | 全部到基线 |
| 工具名大小写 | getUser vs GetUser | 区分大小写 |

---

## STORY-CR-006: 灰度发布状态

### 故事目标
- **核心问题**：验证灰度发布状态正确流转
- **目标用户**：运维工程师
- **预期产出**：状态从 running → paused/completed/rolled_back

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 初始状态 | 创建时 | running |
| 暂停状态 | pause() | paused |
| 完成状态 | complete() | completed |
| 回滚状态 | rollback() | rolled_back |
| 恢复状态 | resume() | running |

---

## STORY-CR-007: 灰度发布暂停

### 故事目标
- **核心问题**：验证 `/api/canary/pause` 正确暂停灰度发布
- **目标用户**：运维工程师
- **预期产出**：暂停后全部请求路由到基线

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 暂停运行中发布 | status=running | 暂停成功 |
| 暂停已暂停发布 | status=paused | 无变更 |
| 暂停已完成发布 | status=completed | 操作失败 |
| 暂停不存在的发布 | 无效 ID | 操作失败 |

---

## STORY-CR-008: 灰度发布恢复

### 故事目标
- **核心问题**：验证 `/api/canary/resume` 正确恢复灰度发布
- **目标用户**：运维工程师
- **预期产出**：恢复后继续按策略分流

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 恢复暂停发布 | status=paused | 恢复成功 |
| 恢复运行中发布 | status=running | 无变更 |
| 恢复已完成发布 | status=completed | 操作失败 |
| 恢复不存在的发布 | 无效 ID | 操作失败 |

---

## STORY-CR-009: 灰度发布完成

### 故事目标
- **核心问题**：验证 `/api/canary/complete` 正确完成灰度发布
- **目标用户**：运维工程师
- **预期产出**：完成后全部请求路由到灰度版本

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 完成运行中发布 | status=running | 完成成功 |
| 完成已暂停发布 | status=paused | 完成成功 |
| 完成已完成发布 | status=completed | 无变更 |
| 完成不存在的发布 | 无效 ID | 操作失败 |

---

## STORY-CR-010: 灰度发布回滚

### 故事目标
- **核心问题**：验证 `/api/canary/rollback` 正确回滚灰度发布
- **目标用户**：运维工程师
- **预期产出**：回滚后全部请求路由到基线版本

### 关键节点/里程碑
1. **起点**：请求回滚灰度发布
2. **步骤1**：验证灰度发布存在
3. **步骤2**：更新状态为 rolled_back
4. **步骤3**：路由全部到基线
5. **终点**：回滚完成

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 回滚运行中发布 | status=running | 回滚成功 |
| 回滚已暂停发布 | status=paused | 回滚成功 |
| 回滚已完成发布 | status=completed | 操作失败 |
| 回滚不存在的发布 | 无效 ID | 操作失败 |

---

## STORY-CR-011: 灰度指标记录

### 故事目标
- **核心问题**：验证灰度指标正确记录
- **目标用户**：运维工程师
- **预期产出**：指标保存到 canary_metrics 表

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 记录请求数 | requestCount | 正确记录 |
| 记录错误率 | errorRate | 正确记录 |
| 记录平均耗时 | avgDuration | 正确记录 |
| 记录成功数 | successCount | 正确记录 |

---

## STORY-CR-012: 灰度指标查询

### 故事目标
- **核心问题**：验证 `/api/canary/metrics` 正确返回指标
- **目标用户**：Dashboard 用户
- **预期产出**：返回灰度发布实时指标

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 查询实时指标 | 有效 ID | 返回当前指标 |
| 查询历史指标 | 时间范围 | 返回历史指标 |
| 查询不存在发布 | 无效 ID | 返回空 |

---

## STORY-CR-013: 灰度发布列表

### 故事目标
- **核心问题**：验证 `/api/canary/list` 正确返回发布列表
- **目标用户**：Dashboard 用户
- **预期产出**：返回灰度发布列表

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 默认查询 | 无参数 | 返回最近 50 条 |
| 按状态筛选 | status=running | 返回运行中发布 |
| 空数据库 | 无发布记录 | 返回空数组 |

---

## STORY-CR-014: 灰度发布详情

### 故事目标
- **核心问题**：验证 `/api/canary/:id` 正确返回发布详情
- **目标用户**：Dashboard 用户
- **预期产出**：返回灰度发布完整信息

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 有效 ID | 存在的发布 | 返回发布详情 |
| 无效 ID | 不存在的发布 | 返回 404 |
| 发布详情 | 查询结果 | 包含 strategy、status、metrics |

---

## STORY-CR-015: 自动回滚阈值

### 故事目标
- **核心问题**：验证错误率超过阈值自动回滚
- **目标用户**：运维工程师
- **预期产出**：错误率 > threshold 自动回滚

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 错误率超阈值 | errorRate > threshold | 自动回滚 |
| 错误率未超阈值 | errorRate < threshold | 继续运行 |
| 无阈值配置 | autoRollbackThreshold=0 | 不自动回滚 |

---

## STORY-CR-016: CanaryRelease 类型

### 故事目标
- **核心问题**：验证 CanaryRelease 类型完整性
- **目标用户**：开发者
- **预期产出**：类型定义正确

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| id 字段 | string | 发布 ID |
| name 字段 | string | 发布名称 |
| strategy 字段 | CanaryStrategy | 筰度策略 |
| status 字段 | CanaryStatus | 发布状态 |
| startTime 字段 | string | 开始时间 |
| endTime 字段 | string? | 结束时间（可选） |

---

## STORY-CR-017: CanaryStrategy 类型

### 故事目标
- **核心问题**：验证 CanaryStrategy 类型完整性
- **目标用户**：开发者
- **预期产出**：策略类型定义正确

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| type 字段 | percentage/user/tool | 策略类型 |
| percentage 字段 | number? | 百分比（可选） |
| userIds 字段 | string[]? | 用户列表（可选） |
| toolNames 字段 | string[]? | 工具列表（可选） |

---

## STORY-CR-018: CanaryMetrics 类型

### 故事目标
- **核心问题**：验证 CanaryMetrics 类型完整性
- **目标用户**：开发者
- **预期产出**：指标类型定义正确

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| requestCount 字段 | number | 请求数 |
| errorCount 字段 | number | 错误数 |
| errorRate 字段 | number | 错误率 |
| avgDuration 字段 | number | 平均耗时 |
| successCount 字段 | number | 成功数 |

---

## STORY-CR-019: 灰度发布并发测试

### 故事目标
- **核心问题**：验证高并发下灰度分流正确
- **目标用户**：性能工程师
- **预期产出**：100 并发请求分流正确

### 测试场景

| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 100 并发 | 同时请求 | 分流比例正确 |
| 百分比验证 | 10% 灰度 | 约 10 个到灰度 |
| 用户策略验证 | 指定用户 | 全部到灰度 |

---

## STORY-CR-020: 灰度发布性能影响

### 故事目标
- **核心问题**：验证灰度分流对请求性能影响
- **目标用户**：性能工程师
- **预期产出**：分流判断耗时 < 1ms

### 测试场景

| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 分流判断耗时 | shouldHitCanary | < 1ms |
| 指标记录耗时 | recordCanaryMetrics | < 5ms |
| 总体性能影响 | 启用 vs 禁用 | 响应时间差 < 10ms |

---

*Generated by 故事线技能 | Date: 2026-04-22*