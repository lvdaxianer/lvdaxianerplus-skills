# CONFIRM-005: 确认弹窗点击背景关闭

## 故事目标

- **核心问题**：验证点击弹窗背景（遮罩）能正确关闭弹窗
- **目标用户**：Dashboard 用户
- **预期产出**：点击遮罩区域触发关闭，效果等同取消

## 关键节点/里程碑

- **起点**：用户点击弹窗外区域（背景遮罩）
- **中间步骤**：
  1. 触发 confirm-modal click event
  2. 检查 e.target.id === 'confirm-modal'
  3. 调用 closeConfirmModal()
- **终点**：弹窗关闭，不执行回调

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| confirm-modal click | 监听点击 | click event | Check target |
| closeConfirmModal | 关闭弹窗 | - | Modal hidden |

## 流程/路径

### 流转条件
点击事件 target 为 confirm-modal 本身（而非内部元素）

### 分支路径
- **条件A** → e.target.id === 'confirm-modal' → 关闭弹窗
- **条件B** → e.target 是内部元素 → 不关闭

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| click event | target | check result | Event listener |
| closeConfirmModal | - | Modal hidden | Function call |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 操作 | 预期结果 |
|------|------|----------|
| 点击背景遮罩 | e.target = modal | 关闭弹窗 |
| 点击弹窗内部 | e.target = modal-box | 不关闭 |
| 点击标题 | e.target = h3 | 不关闭 |
| 点击按钮 | e.target = button | 不关闭（按钮自己处理） |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 点击边缘 | modal 边界 | 关闭弹窗 |
| 点击消息区域 | confirm-message | 不关闭 |

### 用户体验测试场景

#### 提示与反馈
| 场景 | 预期体验 |
|------|----------|
| 点击外部 | 弹窗关闭 |
| 点击内部 | 弹窗保持 |

### 超时处理
无

### 错误恢复机制
无

---

*Generated by 故事线技能 | Date: 2026-04-19*