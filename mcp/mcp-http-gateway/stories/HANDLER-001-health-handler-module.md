# HANDLER-001: Health Handler 模块功能

## 故事目标

- **核心问题**：验证 health.handler.ts 能正确处理健康检查请求
- **目标用户**：监控系统/测试人员
- **预期产出**：GET /health 返回 200 + 服务状态 JSON

## 关键节点/里程碑

- **起点**：请求到达 /health 路径
- **中间步骤**：
  1. Health handler 接收请求
  2. 检查服务运行时间
  3. 获取内存使用情况
  4. 组装健康状态数据
  5. 返回 JSON 响应
- **终点**：返回 { status: 'healthy', uptime: xxx, memory: xxx }

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| Health Handler | 处理健康检查 | Request | Health JSON |
| health.ts | 提供状态数据 | - | uptime, memory |

## 流程/路径

### 流转条件
路由匹配 /health 且方法为 GET

### 分支路径
- **条件A** → 服务正常运行 → status: 'healthy'
- **条件B** → 服务异常 → status: 'unhealthy'

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| getStartTime | - | timestamp | Function call |
| process.memoryUsage | - | memory stats | Node API |
| sendJsonResponse | data | JSON response | HTTP response |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 路径 | 预期结果 |
|------|------|----------|
| GET /health | 正常响应 | 200 + { status: 'healthy' } |
| GET /health/ready | 就绪检查 | 200 + { ready: true } |
| GET /health/live | 存活检查 | 200 + { alive: true } |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 内存异常 | memoryUsage 抛错 | 降级返回部分数据 |
| 启动时间未设置 | startTime = undefined | uptime = 0 |

### 超时处理
响应超时 30s 返回 503

### 错误恢复机制
健康检查失败时返回 503

---

*Generated by 故事线技能 | Date: 2026-04-19*