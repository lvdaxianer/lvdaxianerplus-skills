# HANDLER-002: Tools Handler Mock 配置管理

## 故事目标

- **核心问题**：验证 tools.handler.ts 能正确管理 Mock 配置
- **目标用户**：Dashboard 用户
- **预期产出**：GET/PUT/DELETE Mock 配置正确返回结果

## 关键节点/里程碑

- **起点**：请求到达 /api/tools/:toolName/mock
- **中间步骤**：
  1. 提取 toolName 参数
  2. 检查工具是否存在
  3. 根据方法执行对应操作
  4. GET → 返回当前 Mock 配置
  5. PUT → 更新 Mock 配置
  6. DELETE → 删除 Mock 配置
- **终点**：返回操作结果 JSON

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| Tools Handler | Mock 管理 | toolName, method | Result JSON |
| mock.ts | Mock 存储 | config | Mock data |

## 流程/路径

### 流转条件
路由匹配 /api/tools/:toolName/mock

### 分支路径
- **条件A** → method = GET → 返回配置
- **条件B** → method = PUT → 更新配置
- **条件C** → method = DELETE → 删除配置
- **默认** → 其他方法 → 405

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| parseJsonBody | req.body | JSON object | Body parsing |
| updateMockData | toolName, config | - | Storage update |
| sendJsonResponse | result | JSON | HTTP response |

## 异常/边界情况

### 正常分支测试场景

#### 表单验证
| 字段 | 非空验证 | 长度验证 | 格式验证 |
|------|----------|----------|----------|
| toolName | ✅ 必须 | - | - |
| config JSON | ✅ 必须 | - | JSON 格式 |

#### 业务流程分支
| 条件 | 方法 | 预期结果 |
|------|------|----------|
| GET /api/tools/getUser/mock | 返回配置 | 200 + { mockConfig } |
| PUT + valid JSON | 更新成功 | 200 + { success: true } |
| DELETE /api/tools/getUser/mock | 删除成功 | 200 + { success: true } |

### 异常分支测试场景

#### 输入异常
| 测试类型 | 字段 | 预期处理 |
|----------|------|----------|
| 无效 JSON | body | 400 Bad Request |
| 缺少 toolName | params | 400 Tool not found |

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 工具不存在 | toolName = 'invalid' | 返回空配置或错误 |
| 空配置 | config = {} | 更新为默认配置 |

### 超时处理
无

### 错误恢复机制
JSON 解析失败返回 400

---

*Generated by 故事线技能 | Date: 2026-04-19*