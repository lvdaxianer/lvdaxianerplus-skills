# EJS-001: Dashboard 主模板渲染

## 故事目标

- **核心问题**：验证 EJS 主模板 dashboard.ejs 能正确渲染并包含所有 partials
- **目标用户**：开发者/测试人员
- **预期产出**：Dashboard 页面完整渲染，包含 Overview、Tools、Logs、Stats、Alerts、Config 六个页面

## 关键节点/里程碑

- **起点**：用户访问 `/dashboard` 路径
- **中间步骤**：
  1. HTTP 请求到达 dashboard.handler.ts
  2. 调用 EJS 渲染引擎
  3. 加载 dashboard.ejs 主模板
  4. 依次 include 8 个 partial 文件
  5. 注入数据并返回 HTML
- **终点**：浏览器显示完整 Dashboard 页面

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| HTTP Server | 接收请求 | URL path | Request object |
| Dashboard Handler | 处理渲染 | Config data | HTML string |
| EJS Engine | 模板编译 | Template + Data | Rendered HTML |

## 流程/路径

### 流转条件
请求路径匹配 `/dashboard` 且方法为 GET

### 分支路径
- **条件A** → 模板存在 → 正常渲染
- **条件B** → 模板不存在 → 返回 500 错误

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| Handler | Config | metrics, tools, alerts | Function call |
| EJS | Template + Data | HTML | Template engine |
| Browser | HTML | Rendered UI | HTTP Response |

## 异常/边界情况

### 正常分支测试场景

#### 表单验证
| 字段 | 非空验证 | 长度验证 | 格式验证 |
|------|----------|----------|----------|
| 无表单 | - | - | - |

#### 业务流程分支
| 条件 | 路径 | 预期结果 |
|------|------|----------|
| GET /dashboard | 正常渲染 | 200 + HTML |
| 模板文件存在 | 加载成功 | 完整页面 |
| 数据有效 | 渲染成功 | 包含所有 partials |

#### 时序测试
| 操作 | 防抖/节流 | 参数 | 测试场景 |
|------|-----------|------|----------|
| 页面加载 | 无 | - | 首次渲染时间 < 500ms |

### 异常分支测试场景

#### 输入异常
| 测试类型 | 字段 | 预期处理 |
|----------|------|----------|
| 无效路径 | URL | 404 响应 |

#### 并发测试
| 测试场景 | 操作 | 预期行为 |
|----------|------|----------|
| 并发请求 | 多用户访问 | 正常响应 |

#### 网络异常
| 场景 | 接口 | 处理方式 |
|------|------|----------|
| 请求超时 | /dashboard | 返回错误页面 |

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 空数据 | metrics = {} | 显示空状态 |
| 大数据 | tools > 100 | 分页或截断 |

### 用户体验测试场景

#### 加载状态
| 场景 | 组件 | 预期体验 |
|------|------|----------|
| 页面初始加载 | Dashboard | < 500ms 渲染 |

#### 空状态
| 场景 | 预期体验 |
|------|----------|
| 无数据 | 显示空状态提示 |

### 超时处理
渲染超时 30s 返回 500 错误

### 错误恢复机制
模板错误时返回 fallback HTML

---

*Generated by 故事线技能 | Date: 2026-04-19*