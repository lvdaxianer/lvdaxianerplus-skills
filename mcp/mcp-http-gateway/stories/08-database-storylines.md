# 数据库日志模块故事线

## 故事线概览

本模块包含 10 条故事线，覆盖数据库日志的核心功能。

---

## 故事线 91：SQLite 连接初始化

### 故事目标
- **核心问题**：如何初始化 SQLite 连接
- **目标用户**：系统自身
- **预期产出**：WAL 模式的数据库连接

### 关键节点
1. **起点**：sqlite 配置
2. **中间步骤**：
   - 打开数据库文件
   - 启用 WAL 模式
   - 创建表结构
   - 设置连接池
3. **终点**：数据库连接就绪

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| DbOpener | 数据库打开 | dbPath | database |
| WALEnabler | WAL 启用 | database | WAL 模式 |
| TableCreator | 表创建 | schema | 表结构 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 文件打开 | dbPath | db | better-sqlite3 |
| WAL 设置 | PRAGMA | walMode | exec |
| 表创建 | CREATE TABLE | tables | exec |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 文件不存在 | dbPath 无效 | 创建新文件 |
| 权限不足 | 无法写入 | 进程退出 |

---

## 故事线 92：请求日志写入

### 故事目标
- **核心问题**：如何写入请求日志
- **目标用户**：系统自身
- **预期产出**：请求日志条目存储

### 关键节点
1. **起点**：请求完成
2. **中间步骤**：
   - 构建日志条目
   - 插入 request_logs 表
   - 批量写入优化
   - 异步写入队列
3. **终点**：日志写入完成

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| LogBuilder | 日志构建 | request info | logEntry |
| LogWriter | 日志写入 | logEntry | 写入结果 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 条目构建 | toolName, duration | logEntry | 对象构建 |
| 批量缓存 | logEntry | batch | 数组添加 |
| 数据库写入 | batch | insertResult | INSERT |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 数据库锁定 | LOCKED | 重试写入 |
| 写入失败 | INSERT 错误 | 内存缓存 |

---

## 故事线 93：错误日志写入

### 故事目标
- **核心问题**：如何写入错误日志
- **目标用户**：系统自身
- **预期产出**：错误日志条目存储

### 关键节点
1. **起点**：请求失败
2. **中间步骤**：
   - 构建错误条目
   - 插入 error_logs 表
   - 包含错误详情
   - 关联请求日志
3. **终点**：错误日志写入

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| ErrorLogBuilder | 错误日志构建 | error info | errorEntry |
| ErrorLogWriter | 错误日志写入 | errorEntry | 写入结果 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 条目构建 | error, context | errorEntry | 对象构建 |
| 数据库写入 | errorEntry | insertResult | INSERT |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 错误堆栈过大 | stack 超长 | 截断存储 |
| 关联请求不存在 | requestId 无效 | 独立存储 |

---

## 故事线 94：审计日志写入

### 故事目标
- **核心问题**：如何写入审计日志
- **目标用户**：系统自身
- **预期产出**：审计日志条目存储

### 关键节点
1. **起点**：关键操作执行
2. **中间步骤**：
   - 构建审计条目
   - 插入 audit_logs 表
   - 记录操作详情
   - 脱敏敏感信息
3. **终点**：审计日志写入

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| AuditLogBuilder | 审计日志构建 | operation info | auditEntry |
| SensitiveMasker | 脱敏处理 | data | maskedData |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 条目构建 | operation, result | auditEntry | 对象构建 |
| 脱敏处理 | auditEntry | maskedEntry | 字段替换 |
| 数据库写入 | maskedEntry | insertResult | INSERT |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 脱敏配置禁用 | maskSensitiveFields=false | 原数据存储 |
| 操作详情过大 | details 超长 | 截断存储 |

---

## 故事线 95：告警日志写入

### 故事目标
- **核心问题**：如何写入告警日志
- **目标用户**：系统自身
- **预期产出**：告警日志条目存储

### 关键节点
1. **起点**：告警事件触发
2. **中间步骤**：
   - 构建告警条目
   - 插入 alert_logs 表
   - 记录告警详情
   - 包含阈值信息
3. **终点**：告警日志写入

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| AlertLogBuilder | 告警日志构建 | alert event | alertEntry |
| AlertLogWriter | 告警日志写入 | alertEntry | 写入结果 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 条目构建 | alertType, details | alertEntry | 对象构建 |
| 数据库写入 | alertEntry | insertResult | INSERT |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 告警类型未知 | type 无定义 | 默认类型 |
| 详情缺失 | details 为空 | 最小条目 |

---

## 故事线 96：每日统计聚合

### 故事目标
- **核心问题**：如何聚合每日统计数据
- **目标用户**：系统自身
- **预期产出**：daily_stats 表数据

### 关键节点
1. **起点**：定时任务触发
2. **中间步骤**：
   - 查询当日日志
   - 计算统计指标
   - 更新 daily_stats 表
   - 清理过期数据
3. **终点**：统计聚合完成

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| StatsCalculator | 统计计算 | logs | stats |
| StatsUpdater | 统计更新 | stats | 更新结果 |
| DataCleaner | 数据清理 | maxDays | 清理计数 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 日志查询 | date | logs | SELECT |
| 统计计算 | logs | stats | 聚合函数 |
| 统计更新 | stats | updateResult | INSERT/UPDATE |
| 过期清理 | maxDays | deleteCount | DELETE |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 无当日数据 | logs 为空 | 写入零统计 |
| 清理失败 | DELETE 错误 | 记录错误 |

---

## 故事线 97：日志查询接口

### 故事目标
- **核心问题**：如何提供日志查询 API
- **目标用户**：Dashboard
- **预期产出**：分页查询结果

### 关键节点
1. **起点**：API 请求
2. **中间步骤**：
   - 解析查询参数
   - 构建 SQL 查询
   - 执行查询
   - 返回分页结果
3. **终点**：查询结果返回

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| QueryParser | 查询解析 | params | SQL 条件 |
| QueryExecutor | 查询执行 | SQL | rows |
| Paginator | 分页处理 | rows | 分页结果 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 参数解析 | page, limit, filter | conditions | 对象构建 |
| SQL 构建 | conditions | SQL | 字符拼接 |
| 查询执行 | SQL | rows | db.exec |
| 分页构建 | rows, page, limit | result | 对象构建 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 查询失败 | SQL 错误 | 返回错误 |
| 参数非法 | page/limit 非数字 | 使用默认值 |

---

## 故事线 98：日志保留策略

### 故事目标
- **核心问题**：如何执行日志保留策略
- **目标用户**：系统管理员
- **预期产出**：过期日志自动清理

### 关键节点
1. **起点**：定时清理任务
2. **中间步骤**：
   - 计算 cutoff 日期
   - 删除过期 request_logs
   - 删除过期 error_logs
   - 保留 daily_stats
3. **终点**：过期日志清理完成

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| DateCalculator | 日期计算 | maxDays | cutoffDate |
| LogCleaner | 日志清理 | cutoffDate | 删除计数 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| cutoff 计算 | maxDays | cutoff | Date 计算 |
| request 删除 | cutoff | deletedCount | DELETE |
| error 删除 | cutoff | deletedCount | DELETE |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 删除失败 | DELETE 错误 | 记录错误 |
| 无过期数据 | cutoff 无匹配 | 返回 0 |

---

## 故事线 99：批量写入优化

### 故事目标
- **核心问题**：如何优化批量写入性能
- **目标用户**：系统自身
- **预期产出**：高效的批量写入

### 关键节点
1. **起点**：多个日志条目待写入
2. **中间步骤**：
   - 收集到批次
   - 达到 batchSize 触发写入
   - 使用事务批量 INSERT
   - 清空批次缓存
3. **终点**：批量写入完成

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| BatchCollector | 批次收集 | logEntry | batch |
| TransactionWriter | 事务写入 | batch | 写入结果 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 条目添加 | logEntry | batch | 数组 push |
| 批次检查 | batch.length | shouldWrite | >= batchSize |
| 事务写入 | batch | result | transaction + INSERT |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 批次不满 | length < batchSize | 等待定时写入 |
| 事务失败 | TRANSACTION 错误 | 回滚重试 |

---

## 故事线 100：数据库备份

### 故事目标
- **核心问题**：如何备份数据库文件
- **目标用户**：系统管理员
- **预期产出**：安全的数据库备份

### 关键节点
1. **起点**：备份任务触发
2. **中间步骤**：
   - 复制数据库文件
   - 压缩备份文件
   - 存储到备份目录
   - 清理旧备份
3. **终点**：备份完成

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| DbCopier | 数据库复制 | dbPath | copyPath |
| BackupCompressor | 备份压缩 | copyPath | zipPath |
| OldBackupCleaner | 旧备份清理 | maxVersions | 删除计数 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 文件复制 | dbPath | backupFile | fs.copy |
| 文件压缩 | backupFile | zipFile | gzip |
| 旧备份删除 | maxVersions | deleted | fs.unlink |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 复制失败 | fs 错误 | 记录错误 |
| 压缩失败 | gzip 错误 | 保持原文件 |
| 备份目录满 | maxVersions 达到 | 删除最旧 |

---

*Generated by 故事线技能 | Date: 2026-04-19*