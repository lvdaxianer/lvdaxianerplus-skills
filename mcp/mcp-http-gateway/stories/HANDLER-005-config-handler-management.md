# HANDLER-005: Config Handler 配置管理

## 故事目标

- **核心问题**：验证 config.handler.ts 能正确管理服务配置
- **目标用户**：Dashboard 用户/管理员
- **预期产出**：GET 获取配置，PUT 更新配置，备份和回滚功能正常

## 关键节点/里程碑

- **起点**：请求到达 /api/config
- **中间步骤**：
  1. GET → 返回当前配置
  2. PUT → 更新配置并创建备份
  3. POST /validate → 校验配置有效性
  4. GET /backups → 获取备份列表
  5. POST /restore/:version → 回滚到指定版本
- **终点**：返回操作结果 JSON

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| Config Handler | 配置管理 | method, config | Result JSON |
| config.ts | 配置操作 | configPath | Config data |

## 流程/路径

### 流转条件
路由匹配 /api/config 相关路径

### 分支路径
- **条件A** → GET /api/config → 返回配置 JSON
- **条件B** → PUT /api/config → 更新 + 备份
- **条件C** → POST /api/config/validate → 返回校验结果
- **条件D** → GET /api/config/backups → 返回备份列表
- **条件E** → POST /api/config/restore/:version → 回滚配置

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| loadConfig | configPath | config JSON | File read |
| saveConfig | config | backup version | File write |
| validateConfig | config | { valid, errors } | Schema validation |

## 异常/边界情况

### 正常分支测试场景

#### 表单验证
| 字段 | 非空验证 | 长度验证 | 格式验证 |
|------|----------|----------|----------|
| config JSON | ✅ 必须 | - | JSON Schema |
| version | ✅ 必须 | - | 版本格式 |

#### 业务流程分支
| 条件 | 方法 | 预期结果 |
|------|------|----------|
| GET /api/config | 返回配置 | 200 + config JSON |
| PUT + valid config | 更新成功 | 200 + { success: true } |
| POST /validate + valid | 校验通过 | { valid: true } |
| POST /restore/v1.0 | 回滚成功 | { success: true, version: 'v1.0' } |

### 异常分支测试场景

#### 输入异常
| 测试类型 | 字段 | 预期处理 |
|----------|------|----------|
| 无效 JSON | config body | 400 Bad Request |
| 校验失败 | config | { valid: false, errors: [...] } |
| 版本不存在 | version | 404 Not Found |

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 无备份 | backups = [] | 返回空列表 |
| 配置文件损坏 | read error | 500 Internal Error |

### 用户体验测试场景

#### 提示与反馈
| 场景 | 预期体验 |
|------|----------|
| 保存配置成功 | Toast 提示"配置已保存并自动备份" |
| 回滚成功 | Toast 提示"已回滚到 v1.0" |
| 校验失败 | 显示错误信息列表 |

### 超时处理
配置写入超时返回 500

### 错误恢复机制
配置更新失败时保留原配置

---

*Generated by 故事线技能 | Date: 2026-04-19*