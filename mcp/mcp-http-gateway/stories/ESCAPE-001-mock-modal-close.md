# ESCAPE-001: Escape 键关闭 Mock 配置弹窗

## 故事目标

- **核心问题**：验证 Escape 键能正确关闭 Mock 配置弹窗
- **目标用户**：Dashboard 用户
- **预期产出**：按下 Escape 时 mock-modal 关闭

## 关键节点/里程碑

- **起点**：Mock 配置弹窗处于显示状态
- **中间步骤**：
  1. 用户按下 Escape 键
  2. keydown listener 检测 'Escape'
  3. 调用 closeModal()
  4. 移除 'show' class
- **终点**：Mock 弹窗关闭，配置未保存

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| keydown listener | 监听键盘 | 'Escape' | closeModal() |
| closeModal | 关闭弹窗 | - | Modal hidden |

## 流程/路径

### 流转条件
mock-modal.classList.contains('show')

### 分支路径
- **条件A** → 弹窗显示 + Escape → 关闭
- **条件B** → 弹窗隐藏 + Escape → 无效果

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| keydown | 'Escape' | closeModal() | Event |
| mock-modal | 'show' | class removed | DOM |

## 异常/边界情况

### 正常分支测试场景
| 条件 | 操作 | 预期结果 |
|------|------|----------|
| 弹窗显示 + Escape | closeModal() | 弹窗关闭 |

### 用户体验测试场景
| 场景 | 预期体验 |
|------|----------|
| 键盘快捷键 | Escape 关闭弹窗 |

---

*Generated by 故事线技能 | Date: 2026-04-19*