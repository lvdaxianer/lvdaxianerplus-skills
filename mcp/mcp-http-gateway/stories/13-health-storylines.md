# 健康检查增强功能故事线

> 测试覆盖：K8s 探针、组件状态检查、详细健康报告

---

## STORY-HC-001: 基础健康检查端点

### 故事目标
- **核心问题**：验证 `/health` 端点返回综合健康状态
- **目标用户**：运维工程师
- **预期产出**：返回 7 个组件的健康状态

### 关键节点/里程碑
1. **起点**：请求到达 `/health` 端点
2. **步骤1**：检查 config 配置状态
3. **步骤2**：检查 database 连接状态
4. **步骤3**：检查 circuitBreakers 状态
5. **步骤4**：检查 cache 状态
6. **步骤5**：检查 rateLimit 状态
7. **步骤6**：检查 concurrency 状态
8. **步骤7**：检查 trace 状态
9. **终点**：返回综合健康状态

### 测试场景

#### 正常分支
| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 所有组件正常 | 正常运行 | status: healthy |
| 数据库正常 | 连接成功 | database: healthy |
| 缓存正常 | LRU 缓存可用 | cache: healthy |

#### 异常分支
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 数据库断开 | 连接失败 | database: unhealthy |
| 熔断器打开 | 状态为 OPEN | circuitBreakers: degraded |

---

## STORY-HC-002: 详细健康检查端点

### 故事目标
- **核心问题**：验证 `/health/detail` 返回详细组件状态
- **目标用户**：运维工程师
- **预期产出**：返回每个组件的详细指标

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 详细模式 | `/health/detail` | 包含更多指标 |
| 熔断器详情 | circuitBreakers | 包含各熔断器状态 |
| 限流详情 | rateLimit | 包含当前限流状态 |

---

## STORY-HC-003: K8s 就绪探针

### 故事目标
- **核心问题**：验证 `/health/ready` 检查依赖服务就绪
- **目标用户**：K8s 管理员
- **预期产出**：服务就绪返回 200，否则返回 503

### 关键节点/里程碑
1. **起点**：K8s 发送就绪探针请求
2. **步骤1**：检查数据库连接
3. **步骤2**：检查配置加载完成
4. **终点**：返回就绪状态

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 服务就绪 | 所有依赖可用 | HTTP 200 |
| 数据库未就绪 | 连接失败 | HTTP 503 |
| 配置未加载 | config 为空 | HTTP 503 |

---

## STORY-HC-004: K8s 存活探针

### 故事目标
- **核心问题**：验证 `/health/live` 检查基本存活
- **目标用户**：K8s 管理员
- **预期产出**：存活返回 200，死锁返回 503

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 服务存活 | 正常运行 | HTTP 200 |
| 进程死锁 | 无响应 | HTTP 503（超时） |
| 内存溢出 | OOM | HTTP 503 |

---

## STORY-HC-005: K8s 启动探针

### 故事目标
- **核心问题**：验证 `/health/startup` 支持慢启动容器
- **目标用户**：K8s 管理员
- **预期产出**：启动完成返回 200，启动中返回 503

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 启动完成 | 初始化完成 | HTTP 200 |
| 启动中 | 正在加载配置 | HTTP 503 |
| 启动失败 | 初始化异常 | HTTP 503 |

---

## STORY-HC-006: 组件状态 - Config

### 故事目标
- **核心问题**：验证 config 组件状态检查
- **目标用户**：运维工程师
- **预期产出**：正确返回配置加载状态

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 配置已加载 | config 存在 | healthy |
| 配置为空 | config = null | unhealthy |
| 配置解析错误 | JSON 解析失败 | unhealthy |

---

## STORY-HC-007: 组件状态 - Database

### 故事目标
- **核心问题**：验证 database 组件状态检查
- **目标用户**：运维工程师
- **预期产出**：正确返回数据库连接状态

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 数据库连接正常 | SELECT 1 成功 | healthy |
| 数据库连接失败 | 连接超时 | unhealthy |
| 数据库锁死 | 查询阻塞 | degraded |

---

## STORY-HC-008: 组件状态 - CircuitBreakers

### 故事目标
- **核心问题**：验证 circuitBreakers 组件状态检查
- **目标用户**：运维工程师
- **预期产出**：正确返回熔断器聚合状态

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 全部熔断器 CLOSED | 所有工具正常 | healthy |
| 部分熔断器 OPEN | 部分 OPEN | degraded |
| 全部熔断器 OPEN | 全部 OPEN | unhealthy |

---

## STORY-HC-009: 组件状态 - Cache

### 故事目标
- **核心问题**：验证 cache 组件状态检查
- **目标用户**：运维工程师
- **预期产出**：正确返回缓存状态

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 缓存启用正常 | cache.enabled=true | healthy |
| 缓存禁用 | cache.enabled=false | healthy（允许禁用） |
| 缓存溢出 | 缓存满 | degraded |

---

## STORY-HC-010: 组件状态 - RateLimit

### 故事目标
- **核心问题**：验证 rateLimit 组件状态检查
- **目标用户**：运维工程师
- **预期产出**：正确返回限流状态

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 限流启用正常 | 令牌桶正常 | healthy |
| 限流过载 | 拒绝率高 | degraded |
| 限流禁用 | rateLimit.enabled=false | healthy |

---

## STORY-HC-011: 组件状态 - Concurrency

### 故事目标
- **核心问题**：验证 concurrency 组件状态检查
- **目标用户**：运维工程师
- **预期产出**：正确返回并发状态

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 并发正常 | 当前并发 < 最大值 | healthy |
| 并发满 | 当前并发 = 最大值 | degraded |
| 队列满 | 等待队列满 | unhealthy |

---

## STORY-HC-012: 组件状态 - Trace

### 故事目标
- **核心问题**：验证 trace 组件状态检查
- **目标用户**：运维工程师
- **预期产出**：正确返回追踪状态

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 追踪启用正常 | trace.enabled=true | healthy |
| 追踪禁用 | trace.enabled=false | healthy |
| 追踪数据库失败 | trace_logs 写入失败 | degraded |

---

## STORY-HC-013: 健康状态聚合

### 故事目标
- **核心问题**：验证健康状态聚合逻辑
- **目标用户**：运维工程师
- **预期产出**：正确聚合各组件状态

### 测试场景

| 条件 | 组件状态 | 预期聚合结果 |
|------|----------|--------------|
| 全部 healthy | 7/7 healthy | healthy |
| 部分 degraded | 2 degraded | degraded |
| 有 unhealthy | 1 unhealthy | unhealthy |
| degraded + unhealthy | 混合状态 | unhealthy |

---

## STORY-HC-014: ComponentStatus 类型

### 故事目标
- **核心问题**：验证 ComponentStatus 类型完整性
- **目标用户**：开发者
- **预期产出**：返回正确的 status、message、details

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| status 字段 | healthy/unhealthy/degraded | 枚举值 |
| message 字段 | 描述信息 | 非空字符串 |
| details 字段 | 详细信息 | 可选对象 |

---

## STORY-HC-015: HealthCheckResponse 类型

### 故事目标
- **核心问题**：验证 HealthCheckResponse 类型完整性
- **目标用户**：开发者
- **预期产出**：返回正确的 status、components、timestamp

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| status 字段 | 聚合状态 | 枚举值 |
| components 字段 | 7 个组件 | 包含全部组件 |
| timestamp 字段 | ISO 时间 | 当前时间戳 |

---

## STORY-HC-016: DetailedHealthResponse 类型

### 故事目标
- **核心问题**：验证 DetailedHealthResponse 类型完整性
- **目标用户**：开发者
- **预期产出**：返回详细指标

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 基础字段 | status/components | 包含基础字段 |
| 详细字段 | metrics/details | 包含详细指标 |
| 版本信息 | version | 包含版本号 |

---

## STORY-HC-017: 健康检查路由注册

### 故事目标
- **核心问题**：验证健康检查路由正确注册
- **目标用户**：开发者
- **预期产出**：路由可访问

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| /health 路由 | GET 请求 | 返回健康状态 |
| /health/detail 路由 | GET 请求 | 返回详细状态 |
| /health/ready 路由 | GET 请求 | 返回就绪状态 |
| /health/live 路由 | GET 请求 | 返回存活状态 |
| /health/startup 路由 | GET 请求 | 返回启动状态 |

---

## STORY-HC-018: 健康检查缓存

### 故事目标
- **核心问题**：验证健康检查结果缓存
- **目标用户**：性能工程师
- **预期产出**：避免频繁检查

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 缓存启用 | 短时间内多次请求 | 返回缓存结果 |
| 缓存过期 | 超过 TTL | 重新检查 |
| 缓存禁用 | 无缓存 | 每次检查 |

---

## STORY-HC-019: 健康检查超时

### 故事目标
- **核心问题**：验证健康检查超时处理
- **目标用户**：运维工程师
- **预期产出**：超时返回 degraded

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 检查正常 | 检查时间 < 1s | 正常返回 |
| 检查超时 | 检查时间 > 5s | 返回 degraded |
| 部分超时 | 部分组件超时 | 部分 degraded |

---

## STORY-HC-020: 健康检查并发测试

### 故事目标
- **核心问题**：验证高并发下健康检查稳定性
- **目标用户**：性能工程师
- **预期产出**：100 并发请求正常响应

### 测试场景

| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 100 并发 | 同时请求 | 全部正常响应 |
| 持续并发 | 1 分钟持续请求 | 无内存泄漏 |
| 压力测试 | 1000 请求/秒 | 响应时间 < 100ms |

---

*Generated by 故事线技能 | Date: 2026-04-22*