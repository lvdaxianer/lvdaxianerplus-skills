# CONFIRM-003: 确认弹窗确认操作

## 故事目标

- **核心问题**：验证点击确认按钮能正确执行回调并关闭弹窗
- **目标用户**：Dashboard 用户
- **预期产出**：回调执行，弹窗关闭，操作完成

## 关键节点/里程碑

- **起点**：用户点击"确认"按钮
- **中间步骤**：
  1. 触发 confirm-action-btn click
  2. 检查 confirmCallback 是否存在
  3. 执行 confirmCallback()
  4. 调用 closeConfirmModal()
- **终点**：操作执行完成，弹窗关闭

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| confirm-action-btn | 确认按钮 | click event | Trigger callback |
| confirmCallback | 执行回调 | - | Operation result |
| closeConfirmModal | 关闭弹窗 | - | Modal hidden |

## 流程/路径

### 流转条件
用户选择确认操作

### 分支路径
- **条件A** → confirmCallback 存在 → 执行回调 + 关闭
- **条件B** → confirmCallback 为 null → 仅关闭

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| confirmCallback | stored fn | operation result | Function call |
| closeConfirmModal | - | Modal hidden | Function call |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 操作 | 预期结果 |
|------|------|----------|
| 点击确认 | callback 存在 | 执行回调 + 关闭弹窗 |
| callback 是清空缓存 | async fn | 清空缓存 + Toast 提示 |
| callback 是删除配置 | async fn | 删除配置 + Toast 提示 |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| callback 为 null | confirmCallback = null | 仅关闭弹窗 |
| callback 抛出异常 | fn throws error | 弹窗关闭，错误需处理 |

### 用户体验测试场景

#### 提示与反馈
| 场景 | 预期体验 |
|------|----------|
| 确认操作 | 执行操作并关闭 |
| 操作成功 | Toast 提示成功 |
| 操作失败 | Toast 提示失败 |

### 超时处理
回调执行无超时限制

### 错误恢复机制
回调异常时应捕获并提示

---

*Generated by 故事线技能 | Date: 2026-04-19*