# 配置版本控制功能故事线

> 测试覆盖：版本记录、回滚、比较、导出导入、校验和验证

---

## STORY-CV-001: 配置版本初始化

### 故事目标
- **核心问题**：验证配置版本模块正确初始化
- **目标用户**：运维工程师
- **预期产出**：config_versions 表创建成功

### 关键节点/里程碑
1. **起点**：调用 initConfigVersion
2. **步骤1**：创建 config_versions 表
3. **步骤2**：检查现有版本数
4. **终点**：版本控制就绪

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 正常初始化 | 表不存在 | 创建成功 |
| 表已存在 | 表已创建 | 跳过创建 |
| 数据库失败 | 连接异常 | 初始化失败 |

---

## STORY-CV-002: 配置变更记录

### 故事目标
- **核心问题**：验证配置变更正确记录版本
- **目标用户**：运维工程师
- **预期产出**：每次变更生成新版本号

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 新增配置 | 新工具配置 | 版本号=1 |
| 更新配置 | 更新工具配置 | 版本号+1 |
| 删除配置 | 删除工具配置 | 版本号+1 |
| 批量变更 | 多个工具 | 单个版本记录 |

---

## STORY-CV-003: 版本信息完整性

### 故事目标
- **核心问题**：验证版本记录包含完整信息
- **目标用户**：运维工程师
- **预期产出**：版本记录包含版本号、内容、时间、变更人

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| version 字段 | 版本号 | 自增数字 |
| content 字段 | 配置内容 | JSON 字符串 |
| checksum 字段 | 校验和 | SHA256 |
| createdAt 字段 | 创建时间 | ISO 时间 |
| createdBy 字段 | 变更人 | 用户标识 |
| changeNote 字段 | 变更说明 | 可选描述 |

---

## STORY-CV-004: 配置校验和验证

### 故事目标
- **核心问题**：验证配置校验和防止篡改
- **目标用户**：安全工程师
- **预期产出**：校验和不匹配时拒绝操作

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 校验和匹配 | 原始内容 | 正确匹配 |
| 校验和不匹配 | 篡改内容 | 拒绝操作 |
| 空校验和 | 无校验和 | 计算新校验和 |

---

## STORY-CV-005: 版本列表查询

### 故事目标
- **核心问题**：验证 `/api/config-version/list` 正确返回版本列表
- **目标用户**：Dashboard 用户
- **预期产出**：返回按时间倒序的版本列表

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 默认查询 | 无参数 | 返回最近 50 条 |
| 分页查询 | limit=100 | 返回 100 条 |
| 空数据库 | 无版本记录 | 返回空数组 |
| 时间范围 | startTime/endTime | 筛选版本 |

---

## STORY-CV-006: 指定版本查询

### 故事目标
- **核心问题**：验证 `/api/config-version/:version` 正确返回版本详情
- **目标用户**：Dashboard 用户
- **预期产出**：返回指定版本完整内容

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 有效版本号 | 存在的版本 | 返回版本详情 |
| 无效版本号 | 不存在的版本 | 返回 404 |
| 版本号范围 | 超出范围 | 返回 404 |

---

## STORY-CV-007: 配置回滚

### 故事目标
- **核心问题**：验证 `/api/config-version/rollback` 正确回滚配置
- **目标用户**：运维工程师
- **预期产出**：回滚到指定版本，生成新版本记录

### 关键节点/里程碑
1. **起点**：请求回滚到指定版本
2. **步骤1**：验证目标版本存在
3. **步骤2**：验证校验和匹配
4. **步骤3**：恢复配置内容
5. **步骤4**：生成回滚版本记录
6. **终点**：回滚完成

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 回滚到有效版本 | 存在的版本 | 回滚成功 |
| 回滚到无效版本 | 不存在的版本 | 回滚失败 |
| 回滚校验和不匹配 | 篡改内容 | 回滚失败 |
| 回滚到当前版本 | 当前版本 | 无变更 |

---

## STORY-CV-008: 配置比较

### 故事目标
- **核心问题**：验证 `/api/config-version/compare` 正确比较版本差异
- **目标用户**：Dashboard 用户
- **预期产出**：返回两个版本的差异

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 比较不同版本 | v1 vs v2 | 返回差异列表 |
| 比较相同版本 | v1 vs v1 | 返回无差异 |
| 无效版本 | 不存在的版本 | 返回错误 |
| 多个差异 | 多字段变更 | 返回全部差异 |

---

## STORY-CV-009: 差异格式

### 故事目标
- **核心问题**：验证配置差异格式正确
- **目标用户**：开发者
- **预期产出**：差异包含路径、旧值、新值

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| path 字段 | 变更路径 | JSON path |
| oldValue 字段 | 原值 | 变更前值 |
| newValue 字段 | 新值 | 变更后值 |
| type 字段 | 变更类型 | add/update/delete |

---

## STORY-CV-010: 配置导出

### 故事目标
- **核心问题**：验证 `/api/config-version/export` 正确导出配置
- **目标用户**：运维工程师
- **预期产出**：导出指定版本配置为 JSON 文件

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 导出指定版本 | 有效版本号 | 返回 JSON |
| 导出当前配置 | 无版本号 | 返回当前配置 |
| 导出格式 | JSON 格式 | 格式正确 |

---

## STORY-CV-011: 配置导入

### 故事目标
- **核心问题**：验证 `/api/config-version/import` 正确导入配置
- **目标用户**：运维工程师
- **预期产出**：导入配置并生成新版本

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 导入有效配置 | JSON 配置 | 导入成功 |
| 导入无效配置 | 非 JSON | 导入失败 |
| 导入校验和验证 | 包含校验和 | 验证校验和 |
| 导入并应用 | apply=true | 立即应用 |

---

## STORY-CV-012: 最大版本数限制

### 故事目标
- **核心问题**：验证版本数超过限制自动清理
- **目标用户**：运维工程师
- **预期产出**：超过 maxVersions 自动清理旧版本

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 未达上限 | 版本数 < maxVersions | 不清理 |
| 达到上限 | 版本数 = maxVersions | 清理最旧版本 |
| 超过上限 | 版本数 > maxVersions | 清理多余版本 |

---

## STORY-CV-013: ConfigVersion 类型

### 故事目标
- **核心问题**：验证 ConfigVersion 类型完整性
- **目标用户**：开发者
- **预期产出**：类型定义正确

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| version 字段 | number | 版本号 |
| content 字段 | string | 配置内容 |
| checksum 字段 | string | 校验和 |
| createdAt 字段 | string | 创建时间 |
| createdBy 字段 | string? | 变更人（可选） |
| changeNote 字段 | string? | 变更说明（可选） |

---

## STORY-CV-014: ConfigVersionOptions 类型

### 故事目标
- **核心问题**：验证 ConfigVersionOptions 类型完整性
- **目标用户**：开发者
- **预期产出**：选项类型定义正确

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| maxVersions 字段 | number | 最大版本数 |
| autoCleanup 字段 | boolean | 自动清理 |
| validateChecksum 字段 | boolean | 校验和验证 |

---

## STORY-CV-015: 版本并发访问

### 故事目标
- **核心问题**：验证多用户同时访问版本数据
- **目标用户**：运维工程师
- **预期产出**：并发访问不产生冲突

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 并发读取 | 多用户读取 | 正确返回 |
| 并发回滚 | 多用户回滚 | 最新回滚生效 |
| 并发记录 | 多次变更 | 版本号正确自增 |

---

## STORY-CV-016: 版本记录性能

### 故事目标
- **核心问题**：验证版本记录性能
- **目标用户**：性能工程师
- **预期产出**：记录操作不超过 10ms

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 单次记录 | 1 次变更 | < 10ms |
| 批量查询 | 100 版本查询 | < 50ms |
| 差异计算 | 比较版本 | < 20ms |

---

## STORY-CV-017: 版本数据完整性

### 故事目标
- **核心问题**：验证版本数据完整性
- **目标用户**：运维工程师
- **预期产出**：配置内容正确序列化

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| JSON 序列化 | 配置对象 | 正确序列化 |
| JSON 反序列化 | JSON 字符串 | 正确解析 |
| 特殊字符 | Unicode 字符 | 正确处理 |

---

## STORY-CV-018: 版本记录禁用

### 故事目标
- **核心问题**：验证版本记录禁用时不记录
- **目标用户**：配置管理员
- **预期产出**：禁用后不生成版本记录

### 测试场景

| 条件 | 配置 | 预期结果 |
|------|------|----------|
| 禁用版本控制 | maxVersions=0 | 不记录版本 |
| 启用版本控制 | maxVersions>0 | 正常记录 |

---

## STORY-CV-019: 版本回滚通知

### 故事目标
- **核心问题**：验证回滚操作日志记录
- **目标用户**：运维工程师
- **预期产出**：回滚操作正确记录日志

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 回滚成功 | 回滚完成 | 记录 INFO 日志 |
| 回滚失败 | 回滚失败 | 记录 ERROR 日志 |
| 回滚记录 | 版本记录 | 包含回滚说明 |

---

## STORY-CV-020: 版本历史分析

### 故事目标
- **核心问题**：验证版本历史可追溯分析
- **目标用户**：审计员
- **预期产出**：版本历史完整可追溯

### 测试场景

| 条件 | 测试值 | 预期结果 |
|------|--------|----------|
| 变更追踪 | 变更路径 | 完整追踪 |
| 变更人记录 | createdBy | 记录变更人 |
| 时间线 | 按时间排序 | 时间正确 |

---

*Generated by 故事线技能 | Date: 2026-04-22*