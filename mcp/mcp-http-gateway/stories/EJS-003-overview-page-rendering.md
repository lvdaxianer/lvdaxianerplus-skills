# EJS-003: Overview Page 模板渲染

## 故事目标

- **核心问题**：验证 Overview 页面 partial 能正确展示 aggregated metrics
- **目标用户**：Dashboard 用户
- **预期产出**：总请求、错误率、平均延迟、缓存命中率等指标正确显示

## 关键节点/里程碑

- **起点**：overview-page.ejs 接收 data 对象
- **中间步骤**：
  1. 渲染 aggregated 卡片区域
  2. 显示 totalRequests
  3. 显示 errorRate
  4. 显示 avgDuration
  5. 显示 cacheHitRate
- **终点**：Overview 区域完整渲染

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| Overview Partial | 渲染指标 | data.metrics.aggregated | HTML cards |

## 流程/路径

### 流转条件
data.metrics.aggregated 存在且有效

### 分支路径
- **条件A** → 数据有效 → 显示数值
- **条件B** → 数据无效 → 显示默认值或空状态

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| totalRequests | number | 显示数值 | Template binding |
| errorRate | number | 显示百分比 | Template binding |
| avgDuration | number | 显示毫秒 | Template binding |
| cacheHitRate | number | 显示百分比 | Template binding |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 路径 | 预期结果 |
|------|------|----------|
| totalRequests = 1000 | 正常显示 | "1000" |
| errorRate = 0.05 | 正常显示 | "5%" |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 零值 | totalRequests = 0 | 显示 "0" |
| 负值 | errorRate = -1 | 显示 "0%" |
| 大值 | totalRequests = 1M | 正常显示 |

### 超时处理
无

### 错误恢复机制
数值无效时显示默认值

---

*Generated by 故事线技能 | Date: 2026-04-19*