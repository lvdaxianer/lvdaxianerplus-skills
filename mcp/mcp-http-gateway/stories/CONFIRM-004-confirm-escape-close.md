# CONFIRM-004: 确认弹窗 Escape 键关闭

## 故事目标

- **核心问题**：验证按 Escape 键能正确关闭确认弹窗
- **目标用户**：Dashboard 用户
- **预期产出**：Escape 键触发关闭，效果等同点击取消

## 关键节点/里程碑

- **起点**：用户按下 Escape 键
- **中间步骤**：
  1. 触发 keydown event
  2. 检查 e.key === 'Escape'
  3. 调用 closeConfirmModal()
  4. 移除 'show' class
- **终点**：确认弹窗关闭

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| keydown listener | 监听键盘 | Escape key | Trigger close |
| closeConfirmModal | 关闭弹窗 | - | Modal hidden |

## 流程/路径

### 流转条件
确认弹窗处于显示状态

### 分支路径
- **条件A** → confirm-modal.show 存在 → 关闭弹窗
- **条件B** → 弹窗未显示 → 无效果

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| keydown event | 'Escape' | closeConfirmModal() | Event listener |
| confirm-modal | 'show' | class removed | DOM classList |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 操作 | 预期结果 |
|------|------|----------|
| 弹窗显示 + Escape | 关闭弹窗 | confirm-modal.show 移除 |
| 弹窗隐藏 + Escape | 无效果 | 不影响其他元素 |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 其他按键 | 'Enter', 'Tab' | 不触发关闭 |
| 大小写 | 'escape' vs 'Escape' | 需规范化处理 |

### 用户体验测试场景

#### 响应式/适配
| 场景 | 预期体验 |
|------|----------|
| 键盘操作 | Escape 关闭弹窗 |
| 快捷操作 | 无需点击按钮 |

### 超时处理
无

### 错误恢复机制
无

---

*Generated by 故事线技能 | Date: 2026-04-19*