# ROUTER-002: Exact 精确路径匹配

## 故事目标

- **核心问题**：验证精确匹配（exact）能正确匹配完全相同的路径
- **目标用户**：开发者/测试人员
- **预期产出**：路径完全匹配时返回 matched: true，否则返回 matched: false

## 关键节点/里程碑

- **起点**：matchPath 方法接收到 matchType = 'exact'
- **中间步骤**：
  1. 获取 route.path 字符串
  2. 比较 pathname === route.path
  3. 返回匹配结果
- **终点**：返回 { matched: true/false, params: {} }

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| matchPath | 路径匹配 | pathname, route | Match result |
| Exact matcher | 精确比较 | two strings | boolean |

## 流程/路径

### 流转条件
route.matchType === 'exact'

### 分支路径
- **条件A** → pathname === route.path → matched: true
- **条件B** → pathname !== route.path → matched: false

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| matchPath | pathname, route | result object | Function return |
| params | - | {} | Object property |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 路径 | 预期结果 |
|------|------|----------|
| /health === /health | 匹配 | matched: true |
| /health !== /metrics | 不匹配 | matched: false |
| /api/tools !== /api/tools/test | 不匹配 | matched: false |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 空路径 | pathname = '' | 匹配空路径路由 |
| 大小写 | /Health vs /health | 不匹配 |
| 尾部斜杠 | /health/ vs /health | 不匹配 |

### 超时处理
无

### 错误恢复机制
无

---

*Generated by 故事线技能 | Date: 2026-04-19*