# Dashboard 模块故事线

## 故事线概览

本模块包含 15 条故事线，覆盖 Dashboard 的核心功能。

---

## 故事线 76：Dashboard 页面加载

### 故事目标
- **核心问题**：如何加载 Dashboard 主页面
- **目标用户**：运维人员
- **预期产出**：Bento Grid 风格的 Dashboard 页面

### 关键节点
1. **起点**：浏览器访问 /dashboard
2. **中间步骤**：
   - HTTP Server 接收请求
   - 返回 HTML 页面
   - 加载 CSS 样式
   - 初始化 JavaScript
3. **终点**：Dashboard 页面渲染完成

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| HttpServer | HTTP 服务 | GET /dashboard | HTML |
| DashboardRenderer | 页面渲染 | HTML | DOM |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 请求接收 | GET /dashboard | response | HTTP handler |
| HTML 返回 | dashboard.html | content | sendFile |
| 页面渲染 | HTML | DOM | browser parse |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 页面不存在 | 文件缺失 | 返回 404 |
| 渲染失败 | JS 错误 | 显示错误页面 |

---

## 故事线 77：总览页面请求统计

### 故事目标
- **核心问题**：如何展示请求统计数据
- **目标用户**：运维人员
- **预期产出**：实时请求统计图表

### 关键节点
1. **起点**：Dashboard 加载
2. **中间步骤**：
   - 调用 /api/stats API
   - 查询 SQLite 统计数据
   - 渲染统计卡片
   - 更新图表
3. **终点**：统计数据展示

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| StatsApi | 统计 API | GET /api/stats | JSON |
| StatsQuery | 统计查询 | database | stats |
| ChartRenderer | 图表渲染 | stats | 图表 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| API 调用 | fetch | response | HTTP |
| 数据查询 | SQL | rows | SQLite |
| 图表渲染 | stats | chart | D3/Chart.js |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 数据库无数据 | stats 表空 | 显示空图表 |
| API 失败 | 请求错误 | 显示错误 |

---

## 故事线 78：熔断器状态面板

### 故事目标
- **核心问题**：如何展示熔断器状态
- **目标用户**：运维人员
- **预期产出**：熔断器状态可视化卡片

### 关键节点
1. **起点**：Dashboard 加载
2. **中间步骤**：
   - 调用 /api/circuit-breakers API
   - 获取所有工具熔断器状态
   - 渲染状态卡片
   - 显示 CLOSED/OPEN/HALF_OPEN
3. **终点**：熔断器状态展示

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| CircuitBreakerApi | 熔断器 API | GET /api/circuit-breakers | JSON |
| StateRenderer | 状态渲染 | states | 卡片 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| API 调用 | fetch | response | HTTP |
| 状态获取 | circuitBreakerStates | states | Map 转换 |
| 卡片渲染 | states | cards | DOM 构建 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 无熔断器 | circuitBreaker 禁用 | 显示禁用状态 |
| API 失败 | 请求错误 | 显示错误 |

---

## 故事线 79：缓存状态面板

### 故事目标
- **核心问题**：如何展示缓存状态
- **目标用户**：运维人员
- **预期产出**：缓存统计可视化卡片

### 关键节点
1. **起点**：Dashboard 加载
2. **中间步骤**：
   - 调用 /api/cache API
   - 获取缓存统计
   - 渲染统计卡片
   - 显示大小/容量/命中率
3. **终点**：缓存状态展示

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| CacheApi | 缓存 API | GET /api/cache | JSON |
| CacheStatsRenderer | 统计渲染 | stats | 卡片 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| API 调用 | fetch | response | HTTP |
| 统计获取 | cacheStats | stats | 函数调用 |
| 卡片渲染 | stats | card | DOM 构建 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 缓存禁用 | cache.enabled=false | 显示禁用 |
| API 失败 | 请求错误 | 显示错误 |

---

## 故事线 80：日志列表展示

### 故事目标
- **核心问题**：如何展示请求日志列表
- **目标用户**：运维人员
- **预期产出**：分页日志列表

### 关键节点
1. **起点**：Dashboard 日志页面
2. **中间步骤**：
   - 调用 /api/logs API
   - 查询 SQLite 日志表
   - 分页展示日志
   - 支持筛选/搜索
3. **终点**：日志列表展示

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| LogsApi | 日志 API | GET /api/logs?page=1 | JSON |
| LogsQuery | 日志查询 | page, filter | rows |
| LogsRenderer | 日志渲染 | rows | 列表 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| API 调用 | fetch | response | HTTP |
| 数据库查询 | SQL LIMIT | rows | SQLite |
| 列表渲染 | rows | listItems | DOM 构建 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 无日志数据 | 表空 | 显示空状态 |
| 页码超限 | page > maxPage | 显示最后一页 |

---

## 故事线 81：告警列表展示

### 故事目标
- **核心问题**：如何展示告警日志列表
- **目标用户**：运维人员
- **预期产出**：告警列表和时间线

### 关键节点
1. **起点**：Dashboard 告警页面
2. **中间步骤**：
   - 调用 /api/alerts API
   - 查询 alert_logs 表
   - 显示告警详情
   - 支持告警类型筛选
3. **终点**：告警列表展示

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| AlertsApi | 告警 API | GET /api/alerts | JSON |
| AlertsRenderer | 告警渲染 | alerts | 列表 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| API 调用 | fetch | response | HTTP |
| 数据库查询 | SQL | alerts | SQLite |
| 列表渲染 | alerts | alertCards | DOM 构建 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 无告警 | 表空 | 显示空状态 |
| 告警过多 | 大量数据 | 分页展示 |

---

## 故事线 82：工具列表展示

### 故事目标
- **核心问题**：如何展示工具列表和 Mock 状态
- **目标用户**：运维人员
- **预期产出**：工具列表表格

### 关键节点
1. **起点**：Dashboard 工具页面
2. **中间步骤**：
   - 调用 /api/tools API
   - 获取工具列表
   - 显示 Mock 状态
   - 支持操作按钮
3. **终点**：工具列表展示

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| ToolsApi | 工具 API | GET /api/tools | JSON |
| ToolsRenderer | 工具渲染 | tools | 表格 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| API 调用 | fetch | response | HTTP |
| 数据获取 | config.tools | tools | 直接读取 |
| 表格渲染 | tools | tableRows | DOM 构建 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 无工具 | tools 为空 | 显示空表格 |
| API 失败 | 请求错误 | 显示错误 |

---

## 故事线 83：Mock 配置编辑

### 故事目标
- **核心问题**：如何编辑工具的 Mock 配置
- **目标用户**：运维人员
- **预期产出**：Mock 配置编辑器

### 关键节点
1. **起点**：点击编辑 Mock 按钮
2. **中间步骤**：
   - 打开编辑模态框
   - 加载当前 Mock 配置
   - 编辑静态 JSON
   - 保存配置
3. **终点**：Mock 配置更新

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| ModalOpener | 模态框打开 | click | modal |
| MockEditor | Mock 编辑 | mockConfig | editedConfig |
| MockSaver | Mock 保存 | editedConfig | 更新结果 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 模态框打开 | click | show | DOM 操作 |
| 数据加载 | mockConfig | formData | JSON 解析 |
| 数据提交 | formData | API call | PUT /api/tools/:name/mock |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| JSON 格式错误 | 编辑无效 JSON | 显示错误 |
| 保存失败 | API 返回错误 | 显示错误消息 |

---

## 故事线 84：健康检查展示

### 故事目标
- **核心问题**：如何展示健康检查状态
- **目标用户**：运维人员
- **预期产出**：健康状态可视化

### 关键节点
1. **起点**：Dashboard 加载
2. **中间步骤**：
   - 调用 /health API
   - 获取健康状态
   - 渲染状态卡片
   - 显示各检查项状态
3. **终点**：健康状态展示

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| HealthApi | 健康 API | GET /health | JSON |
| HealthRenderer | 健康渲染 | health | 卡片 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| API 调用 | fetch | response | HTTP |
| 状态解析 | health.status | ok/error | 条件判断 |
| 卡片渲染 | checks | healthCards | DOM 构建 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 服务 unhealthy | status!=healthy | 显示红色状态 |
| API 失败 | 请求错误 | 显示连接失败 |

---

## 故事线 85：配置编辑器页面

### 故事目标
- **核心问题**：如何编辑配置文件
- **目标用户**：系统管理员
- **预期产出**：配置文件在线编辑器

### 关键节点
1. **起点**：Dashboard 配置页面
2. **中间步骤**：
   - 加载当前配置
   - JSON 编辑器
   - 验证配置格式
   - 保存配置
3. **终点**：配置更新成功

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| ConfigLoader | 配置加载 | GET /api/config | JSON |
| ConfigEditor | 配置编辑 | config | editedConfig |
| ConfigValidator | 配置验证 | editedConfig | 验证结果 |
| ConfigSaver | 配置保存 | editedConfig | 保存结果 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 配置加载 | API | currentConfig | HTTP |
| 编辑展示 | currentConfig | editor | JSON 编辑器 |
| 验证调用 | editedConfig | valid | POST /api/config/validate |
| 保存调用 | validConfig | saved | PUT /api/config |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 配置格式错误 | JSON 无效 | 显示错误 |
| 验证失败 | schema 校验失败 | 显示错误 |
| 保存失败 | API 错误 | 显示错误 |

---

## 故事线 86：配置热更新通知

### 故事目标
- **核心问题**：如何通知配置已热更新
- **目标用户**：运维人员
- **预期产出**：配置更新成功提示

### 关键节点
1. **起点**：配置保存成功
2. **中间步骤**：
   - 热更新触发
   - 显示成功消息
   - 刷新相关面板
   - 记录更新日志
3. **终点**：用户确认更新

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| UpdateNotifier | 更新通知 | updateSuccess | 通知消息 |
| PanelRefresher | 面板刷新 | update | 刷新信号 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 成功消息 | saveResult | toast | Toast 显示 |
| 面板刷新 | update | refresh | API 重调用 |
| 日志记录 | updateEvent | log | logger.info |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 热更新失败 | reload 错误 | 显示警告 |
| 部分生效 | 部分配置无效 | 显示详情 |

---

## 故事线 87：响应式布局适配

### 故事目标
- **核心问题**：如何适配不同屏幕尺寸
- **目标用户**：各种设备用户
- **预期产出**：响应式 Dashboard

### 关键节点
1. **起点**：不同屏幕尺寸
2. **中间步骤**：
   - CSS Grid 自适应
   - 卡片大小调整
   - 导航栏适配
   - 表格响应式
3. **终点**：适配完成

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| GridAdapter | Grid 适配 | screen size | 列数 |
| CardAdapter | 卡片适配 | screen size | 卡片大小 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 尺寸检测 | viewport | width | window.innerWidth |
| Grid 调整 | width | columns | CSS Grid |
| 卡片调整 | columns | cardWidth | CSS calc |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 极小屏幕 | width < 320px | 单列布局 |
| 极大屏幕 | width > 1920px | 固定最大宽度 |

---

## 故事线 88：实时数据更新

### 故事目标
- **核心问题**：如何实时更新 Dashboard 数据
- **目标用户**：运维人员
- **预期产出**：自动刷新的数据面板

### 关键节点
1. **起点**：Dashboard 加载完成
2. **中间步骤**：
   - 定时轮询 API
   - 更新统计数据
   - 更新图表
   - 保持连接状态
3. **终点**：数据实时更新

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| PollingTimer | 定时轮询 | interval | 轮询信号 |
| DataUpdater | 数据更新 | API response | 新数据 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 定时触发 | interval | tick | setInterval |
| API 调用 | tick | response | fetch |
| 数据更新 | response | newStats | 状态更新 |
| 图表更新 | newStats | chart | 图表重绘 |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| API 失败 | 请求错误 | 保持旧数据 |
| 网络断开 | 连接失败 | 显示离线状态 |

---

## 故事线 89：错误页面展示

### 故事目标
- **核心问题**：如何展示错误页面
- **目标用户**：所有用户
- **预期产出**：友好的错误提示页面

### 关键节点
1. **起点**：请求/渲染错误
2. **中间步骤**：
   - 捕获错误信息
   - 显示错误页面
   - 提供重试按钮
   - 记录错误日志
3. **终点**：错误页面展示

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| ErrorCatcher | 错误捕获 | error | 错误信息 |
| ErrorRenderer | 错误渲染 | errorInfo | 错误页面 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 错误捕获 | error | errorInfo | catch |
| 页面渲染 | errorInfo | errorPage | DOM 构建 |
| 日志记录 | errorInfo | logEntry | logger.error |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 未知错误 | error 无 message | 默认消息 |
| 严重错误 | 系统崩溃 | 显示重启提示 |

---

## 故事线 90：导航切换

### 故事目标
- **核心问题**：如何实现页面导航切换
- **目标用户**：Dashboard 用户
- **预期产出**：流畅的页面切换

### 关键节点
1. **起点**：点击导航按钮
2. **中间步骤**：
   - 切换导航状态
   - 加载目标页面内容
   - 更新 URL hash
   - 渲染新内容
3. **终点**：页面切换完成

### 角色/参与者
| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| NavHandler | 导航处理 | click | 目标页面 |
| PageLoader | 页面加载 | pageName | 页面内容 |

### 数据流转
| 节点 | 输入数据 | 输出数据 | 传递方式 |
|------|----------|----------|----------|
| 点击响应 | click | targetPage | event handler |
| 状态更新 | targetPage | activeNav | CSS 更新 |
| 内容加载 | targetPage | pageContent | fetch/template |
| URL 更新 | targetPage | hash | history.pushState |

### 异常/边界情况
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| 页面不存在 | page 无定义 | 显示首页 |
| 加载失败 | 内容加载错误 | 显示错误 |

---

*Generated by 故事线技能 | Date: 2026-04-19*