# CONFIRM-001: 确认弹窗显示与内容

## 故事目标

- **核心问题**：验证确认弹窗能正确显示标题、消息和按钮
- **目标用户**：Dashboard 用户
- **预期产出**：弹窗居中显示，包含标题、消息、取消和确认按钮

## 关键节点/里程碑

- **起点**：调用 showConfirmDialog(message, callback, title)
- **中间步骤**：
  1. 设置 confirm-title 元素文本
  2. 设置 confirm-message 元素文本
  3. 保存 callback 到 confirmCallback
  4. 添加 'show' class 到 confirm-modal
- **终点**：弹窗显示在页面中央

## 角色/参与者

| 角色 | 职责 | 输入 | 输出 |
|------|------|------|------|
| showConfirmDialog | 显示弹窗 | message, callback, title | Modal visible |
| confirm-modal | 弹窗容器 | - | DOM element |
| confirmCallback | 回调存储 | callback | Function reference |

## 流程/路径

### 流转条件
危险操作需要确认

### 分支路径
- **条件A** → 提供自定义 title → 显示自定义标题
- **条件B** → 不提供 title → 显示默认"确认操作"

### 循环/回退机制
无

## 数据流转

| 节点 | 输入数据 | 输出数据 | 数据传递方式 |
|------|----------|----------|--------------|
| confirm-title | title text | 显示文本 | DOM textContent |
| confirm-message | message text | 显示文本 | DOM textContent |
| confirmCallback | callback fn | 存储 | Variable |

## 异常/边界情况

### 正常分支测试场景

#### 业务流程分支
| 条件 | 参数 | 预期结果 |
|------|------|----------|
| showConfirmDialog('确定删除?', fn) | 默认标题 | 标题="确认操作" |
| showConfirmDialog('确定删除?', fn, '删除确认') | 自定义标题 | 标题="删除确认" |

### 异常分支测试场景

#### 边界情况
| 类型 | 测试值 | 预期结果 |
|------|--------|----------|
| 空消息 | message = '' | 显示空消息 |
| 超长消息 | message > 100 chars | 消息截断或换行 |
| 无 callback | callback = null | 确认按钮无效 |

### 用户体验测试场景

#### 提示与反馈
| 场景 | 预期体验 |
|------|----------|
| 弹窗显示 | 居中显示，背景遮罩 |
| 标题清晰 | 明确操作类型 |
| 消息明确 | 说明操作后果 |

### 超时处理
无

### 错误恢复机制
弹窗 DOM 错误时不影响主流程

---

*Generated by 故事线技能 | Date: 2026-04-19*